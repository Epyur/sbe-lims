# AGENTS.md — sbe-lims (ЛИМС)

SBE-плагин «ЛИМС» для сотрудников лабораторий: заявки лаборатории (по `lab_members`),
результаты испытаний по сериям, расчёты (DSL-формулы), классификация, статистика, графики,
протоколы (HTML/docx), справочники лаборатории. Клиент lab-service (сервер — канон).
**Дашборд из плагина удалён (2026-08-18)** — будет вынесен в отдельный плагин; серверный
эндпоинт `GET /api/lab/dashboard` оставлен невостребованным (не удалять).

**Бэк — в этой же папке** (`lab-service/`, 2026-08-24, переехал из `server_back/lab-service/`) —
на отдельной ветке `backend` (main этого репозитория — чистый релизный срез кода плагина,
без бэка; см. правило «Бэки в папках плагинов» в корневом `plugins/AGENTS.md`). `lab-service`
общий и для sbe-requests — его `AGENTS.md` содержит указатель сюда.

## Назначение (текущее)

- **Источник данных** — lab-service `https://epyur.fvds.ru` через JWT из ЦУП СБЕ
  (`getService('sbe-apstore').auth.getToken('lab')`, app_id `lab`). Прямые запросы к
  `/api/lab/*` (без локального БД-кэша справочников; заявки/результаты — в памяти вьюхи).
- **Заявки**: список доступных заявок (`GET /requests` — сервер фильтрует по видимости:
  владелец / участник группы / admin / сотрудник лаборатории по `lab_members`), карточка
  с сериями результатов, сменой статуса (`new/received/processing/completed`), графиками
  и протоколом.
- **Результаты**: ввод серии (параметр=значение), сохранение (`POST /results`) —
  сервер автоматически выполняет DSL-формулы/классификацию/статистику; пересчёт серии
  (`POST /results/{series}/calculate`); агрегаты (`GET /results/aggregated`).
- **Справочники**: методы (создание + конфиги formulas/classification/chart_configs/
  input_parameters через JSON-редактор + удаление, всё — admin), испытатели
  (CRUD editor+), оборудование (CRUD editor+), сотрудники лабораторий
  (`lab-members`, admin).
- **Графики**: `GET /requests/{id}/chart/{cfg_id}` → PNG (сервер рендерит по
  `methods.chart_configs`), показываются в карточке заявки.
- **Протокол/выписка/короткий вид** (2026-08-23, блочный редактор — заменил секции
  от 2026-08-22): `POST /requests/{id}/protocol?template=ui|excerpt|protocol&format=html|full`
  → `{html, docx_base64}` (`template` по умолчанию `protocol`; `format=html` — не тратить
  время на DOCX, для карточки результатов/предпросмотра). Документ — блоки форматированного
  текста (абзацы/заголовки/списки/таблицы) с плейсхолдер-чипами (системные данные заявки/
  объекта, атрибуты метода — эксперимента со сверткой avg/min/max/first/last вне таблицы,
  агрегированные как есть); карточка результатов заявки и read-only блок в sbe-requests
  вставляют HTML из `protocol?template=ui&format=html` — прежний отдельный JSON-эндпоинт
  `GET .../short-view` удалён.
- **Точка входа** — магазин: «Установленные → Открыть» (`publishService('sbe-lims', {open})`).
- Вид открывается из ЦУП (`SbeLimsApi extends SbeOpenableApi`), без ribbon.

## Правило: системные атрибуты (2026-08-23)

Данные, общие для ЛЮБОГО метода испытаний (не специфичные для конкретной методики),
заводятся **ОДИН РАЗ в общий каталог**, а не как `MethodAttribute` per-method. Прямое
решение пользователя: "у нас же инвентор он системный, как и другие перечисленные,
зачем их настраивать под каждый метод, заведи эти атрибуты как универсальные, так же
как например «Наименование материала»". Найдено кросс-проверкой реальных писем
Comb/Flam/FlamProp (`json_attr.md`, `mail_records.jsonl`) — метод ГВ по ошибке завёл
`amb_temp`/`amb_pres`/`amb_moist`/`exp_date` как СВОИ атрибуты, хотя эти поля несёт
КАЖДОЕ письмо-результат независимо от метода.

**Текущий каталог** (`SYSTEM_PLACEHOLDERS`, `sbe-lims/src/ui/block-editor.ts`):
`title`/`number`/`object_name`/`ekn`/`owner_email`/`priority`/`test_purpose`/`status`/
`created_at`/`customer_number`/`lab_number`/`target_indicator`/`batch_number`/
`sample_id`/`thickness` (заявка/объект, известны сразу) + `inventor`/`report_date`/
`samples_in_date`/`exp_date`/`amb_temp`/`amb_pres`/`amb_moist` (испытатель/даты/
условия среды — заполняются автоматически из письма-результата, `requests.*`).

**Что автоматически получает новое системное поле** (три поверхности, каталог
дублирован вручную в трёх местах — синхронизировать при добавлении):
1. Доступно как плейсхолдер-чип в блочном редакторе протокола (`SYSTEM_PLACEHOLDERS`
   → `PlaceholderPickerModal`), резолвится сервером (`resolveSystemPlaceholder`,
   lab-service `protocol.go`).
2. **НЕ заводится как `MethodAttribute`** — конфигуратор метода показывает справку
   со списком системных полей прямо над блоком «Атрибуты метода» (переиспользует тот
   же `SYSTEM_PLACEHOLDERS`), чтобы админ не дублировал их вручную на каждом методе.
3. **Автоматически показывается в форме для испытателя** (`renderOperatorFormPreview`)
   — отдельным read-only разделом «Системные данные», ВСЕГДА, независимо от того,
   какие поля админ добавил в `operator_form.fields` для этого метода конкретно;
   `operator_form.fields` содержит только метод-специфичные поля.

**Отличие от метод-специфичных полей с одинаковым ИМЕНЕМ**: не всякое повторяющееся
raw-имя письма — системное. `additional_info`/`substrate`/`mounting_method` тоже
встречаются у нескольких методов (Comb/Flam), но их СОДЕРЖАНИЕ метод-специфично
(разное описание подложки/крепления/наблюдений) — остаются обычными `MethodAttribute`
на каждом методе, где нужны. Критерий системности — значение ОБЪЕКТИВНО одно и то же
независимо от метода (кто испытывал, когда, какая была температура в лаборатории),
а не просто совпадение имени поля в письме.

**Реализация на сервере** (`lab-service`, 2026-08-23): новые колонки `requests.*`
(`inventor_id` FK на `inventors`, `report_date`/`samples_in_date`/`exp_date`/
`amb_temp`/`amb_pres`/`amb_moist` TEXT) вместо generic `values` каждой серии;
`email_ingest.go` — `systemRequestFields` перехватывает канонические имена ДО того,
как они попали бы в атрибуты метода, `applyRequestSystemFields` резолвит имя
испытателя в `inventor_id` точным совпадением по `inventors.name` (не создаёт
испытателя автоматически). См. lab-service `AGENTS.md` за деталями.

## Структура

| Файл | Что это |
|---|---|
| `src/main.ts` | `SbeLimsPlugin`: настройки, syncService, refreshMethods (pull методов при onload), view, publishService |
| `src/services/sync.service.ts` | `LimsSyncService`: JWT из ЦУП, заявки/результаты/расчёт/справочники/графики/протокол, `moveKanbanCard` (2026-08-24, `POST .../kanban-move`), `listLabMembers(labId?)` (без аргумента — полный список, admin; с `lab_id` — ростер одной лабы, доступен любому её участнику), permissions/me, таймауты 30с, понятные 401/403 |
| `src/ui/lims-view.ts` | `LimsView` (тип `sbe-lims-view`): фасад (шапка + сайдбар-дерево + контент). «Все заявки»/«Результаты и протоколы» — один рендер `renderRequests(filter?)` с разными фильтрами по статусу; **«Очередь лаборатории»** (2026-08-24) — Kanban-доска `renderQueueBoard()`, 4 колонки new/received/processing/completed, колонки 2/3 делятся на ячейки по испытателям лабы (`renderPerTesterKanbanColumn`), drag-and-drop + контролы в детали заявки идут через один `performKanbanMove()`; карточка заявки (серии/расчёт/статус-назначение/графики/протокол), «Методы» (список + JSON-редактор конфигов, admin), «Объекты» (только чтение), «Испытатели»/«Оборудование» (список + создание, editor+), «Сотрудники» (список + добавление/удаление, admin) |
| `src/ui/settings-tab.ts` | Настройки: apiUrl + «Права доступа» (роли + общий доступ) |
| `src/types/lims.ts` | `LimsRequest` (+ `assigned_to`/`completed_at`, 2026-08-24, Kanban-доска), `Lab`, `LabMethod`, `LabObject`, `MeasurementResult`, `AggregatedResult`, `Inventor`, `Equipment`, `LabMember`, `MethodConfig`, `ProtocolResponse`, `DocumentBlock`/`RichNode`(+`align`/`static_table`/`rows`, 2026-08-24)/`InlineNode`(+`sup`/`sub`, 2026-08-24)/`TableColumn`/`PlaceholderSource`/`PlaceholderAgg`, `OperatorFormField`/`MethodOperatorForm` |
| `src/ui/block-editor.ts` | `renderBlockEditor` — визуальный (WYSIWYG) редактор ОДНОГО блока: строки абзац/заголовок/список/таблица/**статическая таблица** (2026-08-24, `renderStaticTableEditor` — визуальный конструктор, ячейки — mini rich-text), каждая текстовая строка — небольшой `contenteditable` с тулбаром Ж/К/**x²/x₂** (2026-08-24, `execCommand('superscript'/'subscript')`)/🏷Плейсхолдер (чип, `PlaceholderPickerModal`); абзац/заголовок — **выравнивание** (`renderAlignSelect`, 2026-08-24); таблица данных серий — структурный виджет (не contenteditable), колонки атрибутов + "Серия", drag-reorder; **список — drag-reorder пунктов** (2026-08-24) |
| `src/ui/subsup.ts` | `toggleSubSupPalette` (2026-08-24, вынесен из `lims-view.ts`) — юникод-палитра над-/подстрочных символов для plain `<input>` (название атрибута, подпись колонки таблицы) |
| `src/styles.css` | Классы `tn-lims-*` на семантических токенах, `.tn-lims-chip` (плейсхолдер), `.tn-lims-rich-line` (contenteditable-строка), `.tn-lims-kanban*` (2026-08-24, доска) |

## Настройки (data.json)

`apiUrl` (default `https://epyur.fvds.ru`). Роли lab (апп-шкала lab_permissions):
`viewer`(1) < `editor`(2) < `admin`(3) < `superadmin`(4, реализована 2026-08-19).
Лабораторный скоуп — `lab_members(lab_id, email, role)`: `lab_operator` / `lab_admin` /
`lab_auditor` (реализована 2026-08-19, см. таблицу ниже).

## Модель прав ЛИМС (переработка согласована 2026-08-18, основное реализовано 2026-08-19)

| Роль | Где | Возможности | Статус |
|---|---|---|---|
| **superadmin** | `lab_permissions` (новая, выше admin) | Создание лабораторий, назначение admin/superadmin. Видит все лаборатории | ✅ реализовано (сервер) |
| **admin** | `lab_permissions` (существующая) | Видит все лаборатории | ✅ (создание лабораторий у admin забрано — теперь только superadmin) |
| **editor / viewer** | `lab_permissions` (существующие, app-level) | Базовые app-level способности | не менялось |
| **lab_admin** | `lab_members` (per-lab) | *Задумано*: управляет своей лабораторией (добавляет участников, правит методы, назначает админов) | ⚠️ **не реализовано** — сервер сейчас не отличает `lab_admin` от `lab_operator` (оба дают только запись результатов через `requireLabAccess`); делегированные полномочия внутри своей лабы без app-level admin — отдельная задача авторизации по ресурсу, не сделана |
| **lab_operator** (испытатель) | `lab_members` (per-lab) | Проводит испытания, вносит данные | ✅ (`requireLabAccess`, как было) |
| **lab_auditor** | `lab_members` (новая, per-lab) | Доступ ко всем разделам своей лаборатории, только чтение | ✅ реализовано (`requireLabRead`: результаты/графики/протокол читает, писать не может) |

Сделано на сервере 2026-08-19 (см. lab-service/AGENTS.md, запись «модель ролей»):
роль `superadmin` (`roleRank`, защита от самоповышения в `handleSetPermission`,
`seedOwner` теперь ставит владельца в superadmin), роль `lab_auditor` + разделение
`requireLabAccess`(запись)/`requireLabRead`(чтение), фильтрация `GET /labs` по
видимости, `POST /labs` → superadmin-only. Плагин: `canAdmin`/`canEditRefs` в
`lims-view.ts` учитывают `superadmin`; `settings-tab.ts` (оба — sbe-lims и sbe-requests)
пускают superadmin к панели прав и добавляют `lab_auditor`/`superadmin` в списки ролей.

⚠️ **Известный пробел, не исправлен**: `POST /requests/{id}/status` проверяет только
видимость заявки (`requestVisible`), не write-право — теоретически статус может
сменить любой, кому заявка видна (например, участник группы), не только
lab_operator/lab_admin/владелец. Не трогали: эндпоинт общий с sbe-requests, а
правильная политика («кто должен мочь менять статус») не зафиксирована ни в одной
спеке — фикс без явного решения рисковал бы регрессией у обоих потребителей.

## Правила

- `catch(e: unknown)` + `errorMessage()`; `requestUrl()` (в `Promise.race` с
  `window.setTimeout`); без `any`; UI на русском; автор — Полищук Евгений (polishchuk@tn.ru).
  Классы `tn-lims-*` / `tn-btn*` / `tn-table` на семантических токенах sbe-core.
- Коммиты/пуши — только по явной команде пользователя.
- **«Фиксируй» = поднять версию (+0.0.1 в `manifest.json` и `package.json`), обновить
  документацию, подготовить сообщение для коммита и СПРОСИТЬ подтверждение commit/push.**
  Не поднимать версию плагина при коммите чисто бэковых изменений в `lab-service/`
  (ветка `backend`), если сам плагин не менялся.
- **Никогда и нигде не использовать «лучше»/«хуже» (и производные: best/worst,
  лучший/худший)** — ни в UI, ни в коде/идентификаторах, ни в комментариях/документации
  (2026-08-22, явное указание пользователя: это оценочные категории, неприменимые к
  объективной оценке результатов испытаний). Только нейтральные сравнительные термины:
  «выше/ниже», «больше/меньше», «максимальное/минимальное». Пример: DSL-функции
  `min_grade`/`max_grade` (не `worst_grade`/`best_grade`), агрегация правил
  классификации `none`/`avg`/`min`/`max` (не `best`/`worst`).

## ⚠️ Текущий статус — дорожная карта MVP (2026-08-28)

Роадмап: `docs/superpowers/plans/2026-08-27-mvp-roadmap-plan.md` (вне git,
корень `.obsidian/plugins/`). Порядок запуска пакетов — прямое решение
пользователя: **WP1 → WP2 →** далее по обстановке.

**Готово:**
- **WP3d (TDT Reader) — ПОЛНОСТЬЮ ГОТОВО.** Финальная схема: прибор не вводит
  номер заявки/серии — считает hash, шлёт `{hash,values}` в буфер
  (`POST /api/lab/instrument-buffer`), показывает hash+QR; испытатель
  вставляет hash в форму результатов, сервер атомарно заявляет буфер и мержит
  данные. Backend закоммичен и запушен (`5131437`/`b10fb49`, ветка
  `backend`), живой E2E с РЕАЛЬНЫМ hash от настоящего TDT Reader — нашёл и
  исправил баг (claim без отката при неудаче сохранения, см.
  `lab-service/AGENTS.md`). Python-приложение (`C:\python_dev\tdt_reader\TDT
  Reader\`) переписано, закоммичено и запушено (`3a90cfc`), пользователь уже
  прогнал реальный эксперимент и получил hash через QR — весь ручной чек-лист
  закрыт.
- **WP4 упрощён** (по решению пользователя): строгая проверка прав
  редактирования не в MVP — «редактировать могут все испытатели и все
  администраторы лаборатории».
- **WP1 (калибровочная кривая метода РП) — ПОЛНОСТЬЮ ГОТОВО, закоммичено и
  запушено** (backend `644fd29` ветка `backend`, перенесено в `main` `c05abc8`;
  `sbe-lims-mobile` `286d52a`). Подробности — История работ ниже (v0.2.20) и
  `lab-service/AGENTS.md`.
- **WP2 (исходящая почта + LPITrack) — ПОЛНОСТЬЮ ГОТОВО** (брейнсторм → спека →
  план → реализация → живой E2E в этой же сессии, включая найденный и
  исправленный инфраструктурный баг с `host.docker.internal`/`extra_hosts`).
  Подробности — История работ ниже (v0.2.21) и `lab-service/AGENTS.md`.
- **WP3 (форма испытателя, все части 3a/3b/3c-ч.1/3c-ч.2/3d) — ПОЛНОСТЬЮ
  ЗАКРЫТ**, включая редизайн таймера/рекомендуемые значения 2026-08-29 (см.
  История работ ниже, v0.2.26).
- **WP5 («№ серии» → «№ п/п») — ПОЛНОСТЬЮ ГОТОВО (2026-08-29)** — новый
  `TableColumn.kind="sequential"` (та же нумерация i+1, что `series_no`, своя
  подпись «№ п/п»). Подробности — История работ ниже и `lab-service/AGENTS.md`.
- **WP4 — ПОЛНОСТЬЮ ГОТОВО (2026-08-29)** — реальный UI редактирования серии
  уже был (`renderSeriesEditForm`, WP3a); добавлен последний недостающий кусок
  — `measurement_results.created_by`/`updated_by`. Подробности — История работ
  ниже и `lab-service/AGENTS.md`.
- **WP6 (нумерация заголовков) — ПОЛНОСТЬЮ ГОТОВО (2026-08-29)** — путь (б)
  (плейсхолдер номера внутри вручную набранного текста, решение пользователя).
  Подробности — История работ ниже и `lab-service/AGENTS.md`.

- **WP7 (финальная валидация email-приёма) — ГОТОВО (2026-08-29)** —
  пользователь включил постоянный воркер, реальный бэклог (21 заявка)
  обработан без ручного вмешательства, протокол собирается. Подробности —
  `lab-service/AGENTS.md`.
- **Аудит истории email-приёма + массовый повторный разбор — ГОТОВО
  (2026-08-29)** — по запросу пользователя свели пропуски данных в CSV,
  нашли: из 2731 обработанного за всю историю писем только 716 применились
  успешно. Причины — легаси-формат без поля `type` (1278 писем, тип/метод
  определялись папкой, не полем) и незаведённый `method2`→ГВ в
  `LAB_MAIL_METHOD_MAP` (585 писем). Обе причины исправлены и повторно
  разобраны (backup БД перед началом, батчами) — +74 новые заявки чистым
  приростом, «завершённые заявки без единой серии» упало с 297 до 32.
  Backend почти не менялся (только этот компонент), клиент не затронут —
  подробности, живой E2E, точный ход массового разбора —
  `lab-service/AGENTS.md`.
- **Фикс калибровочной кривой метода РП + массовый пересчёт всех заявок —
  ГОТОВО (2026-08-29)** — живая жалоба на заявку 286/2026-РП: формула КППТП
  (предложена ИИ-помощником) ссылалась на несуществующие параметры `xs`/`ys`
  вместо реальных `heat_flow_curve_xs`/`heat_flow_curve_ys`; вдобавок найден
  реальный баг — калибровочные кривые вообще не подставлялись на
  aggregated-уровне формул (только на уровне серии). Оба исправлены; новая
  CLI-команда `recalc-all` прогнана по всей БД (453 пары заявка+метод, без
  ошибок). Клиент не затронут — подробности, проверка на живых данных,
  побочно замеченный (не исправленный, вне объёма) пробел по методу ГГ —
  `lab-service/AGENTS.md`.
- **Уведомление в LPITrack при переходе в «В работе»; ручной адрес служебных
  писем per-лаба; фикс темы служебного письма — ГОТОВО (2026-08-29)** —
  живой E2E первой исходящей почты (заявка 428/2026-ГВ) вскрыл: (a) не баг —
  `auto_send_email` лабы был выключен, пользователь включил сам; (b) новый
  триггер уведомления при взятии заявки в работу; (c) новое поле
  `labs.service_email` — ручной адрес вместо жёсткого env; (d) тема письма
  теперь совпадает с полем «Внешний идентификатор» (`LPIZAYAVKINAPRO-<N>`).
  Клиент — только новое поле в форме лабы. Подробности, что ещё нужно
  проверить пользователю живьём — `lab-service/AGENTS.md`.
- **Исходящая почта — аутентифицированная отправка через тот же внешний ящик
  — ГОТОВО (2026-08-29)** — живая жалоба: письма через локальный exim-релей с
  самоподписанным сертификатом попадали в спам. Переведено на `smtp.PlainAuth`
  тем же `LAB_MAIL_LOGIN`/`LAB_MAIL_PASSWORD`, что уже читает почту (реальный
  провайдер Yandex, валидный сертификат/SPF/DKIM). Проверено сетевым
  E2E-тестом транспорта (SMTP AUTH успешен), не проверено — реальная отправка
  через сам код и итог по спаму у получателя. Клиент не затронут — подробности
  — `lab-service/AGENTS.md`.
- **WP8 (журнал изменений заявки/результатов) — ГОТОВО (2026-08-29)** —
  спека и план реализации одобрены пользователем и реализованы сразу следом.
  Новая таблица `request_audit_log`, точки записи (смена статуса — три места;
  результаты — `saveResultSeries`, с явным разделением «Пересчитать»
  (логируется) vs CLI `recalc-all` (не логируется)), `GET .../audit-log`,
  раздел «История» в карточке заявки (сворачиваемый, подгружается по клику) —
  в обоих плагинах (`sbe-lims` и `sbe-requests`). Живой E2E backend-части
  (recalc-all на всей БД — 0 строк журнала, как и должно быть) — есть; полный
  путь через реальный HTTP/UI — нет (нет скриптового JWT для проверки извне).
  Подробности — `lab-service/AGENTS.md`.
- **WP9 (справка в плагинах) — ГОТОВО (2026-08-29). ВЕСЬ ПЛАН MVP ЗАКРЫТ
  (WP1–WP9).** Спека — `docs/superpowers/specs/2026-08-29-sbe-lims-sbe-requests-help-design.md`.
  Тот же паттерн, что уже был в `sbe-requests` (markdown-константа + заметка в
  вольте по кнопке «?») — теперь и в `sbe-lims` (новый `src/ui/help.ts`,
  практический гайд по разделам навигации плагина, без справочника по
  синтаксису DSL/JSON-классификации). Версия обоих плагинов — **1.0.0** (по
  прямому решению пользователя, отмечает завершение MVP; обычное правило
  «следующая версия — 0.х+1» не применялось намеренно).

**Открыто:** нет — план MVP полностью выполнен. Дальше — пакеты «Желательно»
(объединение ячеек в таблицах, единицы давления, шрифты/регистр вставки), если
понадобятся, и всё, что появится по факту использования MVP пользователем.

## История работ

### 2026-09-04 — main синхронизирован — версия 1.0.4 (снят суффикс b)

Перенос плагин-части v1.0.4b в `main` выполнен (см. его `AGENTS.md`,
«перенос из backend»); суффикс `b` снят на обеих ветках.

### 2026-09-04 — v1.0.4b (4-й вид представления «Справка»; узкий эндпоинт для целевого показателя — задеплоено на VDS, перенос в main пока НЕ выполнен по решению пользователя)

Две задачи пользователя этой сессии:
1. **«Справка» — 4-й флаг блока** в конфигураторе представления метода (рядом
   с «Кратко»/«Выписка»/«Протокол») — содержимое отмеченных блоков теперь
   идёт во всплывающую подсказку веб-портала над «Не соответствует» (раньше
   там ошибочно жила «Выписка», другого назначения). См.
   `lab-service/AGENTS.md` за детали `ShowInHelp`/`template=help`.
2. **Владелец заявки может дозаполнить целевой показатель** (веб-портал,
   `sbe-web`) — новый серверный узкий эндпоинт, из этого плагина напрямую не
   вызывается (используется только веб-клиентом), но живёт в общем
   `lab-service`.

По прямому решению пользователя: `backend` НЕ переносится в `main` в этой
сессии (главная ветка продолжает отдавать 1.0.3) — задеплоен только сервер
(`lab-service`), суффикс `b` остаётся до отдельной команды на перенос.

`npx tsc --noEmit` EXIT=0, `npm run build` OK. Версия 1.0.3 → **1.0.4b**.

### 2026-09-04 — main синхронизирован — версия 1.0.3 (снят суффикс b)

Перенос плагин-части v1.0.3b в `main` выполнен (см. его `AGENTS.md`,
«перенос из backend»); суффикс `b` снят на обеих ветках.

### 2026-09-04 — v1.0.3b (фильтр по статусу списка заявок; удаление проектов/групп — этот плагин не участвует, см. lab-service/AGENTS.md; «Результат»/«Соответствие» — тоже только бэкенд+веб)

Из трёх задач пользователя этой сессии плагин затронула только одна: **фильтр
по статусу** («Все»/«Активные»/«Завершённые») добавлен в панель фильтров
(`renderRequestsFilterBar`/`matchesRequestFilters`) — «Активные» значит
`status !== 'completed'`. Удаление проектов/групп и вывод «Результат»/
«Соответствие» этот плагин не касаются (sbe-lims не управляет проектами/
группами — это зона `sbe-requests`; «Результат»/«Соответствие» добавлены
только в веб-версию, см. `sbe-web/AGENTS.md`). Бэкенд (`lab-service`) на этой
же сессии получил новые `DELETE /projects/{id}`/`.../groups/{id}` и
автоопределение пары «Результат/Соответствие» по структуре правила
классификации — см. `lab-service/AGENTS.md`, эти правки не требуют изменений
в этом плагине.

`npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 1.0.2 → **1.0.3b**
(суффикс `b` — задет и бэкенд, ещё не перенесено на `main`).

### 2026-09-04 — main синхронизирован — версия 1.0.2 (снят суффикс b)

Перенос плагин-части v1.0.2b в `main` выполнен (см. его `AGENTS.md`,
«перенос из backend»); суффикс `b` снят на обеих ветках — `main` и `backend`
снова совпадают по номеру версии. Backend (`lab-service`) продолжает жить
только здесь, на `backend`.

### 2026-09-04 — v1.0.2b (фильтры списка заявок; настройки лаборатории для оповещений о поверке, доступ lab_admin)

Три задачи пользователя за сессию (см. также `sbe-requests`/`sbe-web` — тот же
день, паритетные правки в других клиентах):

1. **Панель фильтров** в списке заявок (`renderRequests`, было вообще без UI
   фильтрации): дата регистрации (диапазон), метод, название объекта,
   идентификатор (`customer_number`/`lab_number`/`external_id`/`id`), партия
   (`object.characteristics.batch_number`), заказчик (`owner_email` — по
   согласованию с пользователем, отдельного поля «заказчик» в модели нет).
   Комбинируются по И с уже существующими caller-предикатами (статус
   `completed` у «Результаты», `object_id` у «→ Заявки»). Дебаунс текстовых
   полей — 1200мс (по просьбе пользователя). Тот же паттерн — в `sbe-requests`.
2. **Оповещения о сроке поверки → «настройки лаборатории», доступ lab_admin.**
   Раздел «Уведомления о сроке поверки» перенесён из Obsidian
   `PluginSettingTab` в facade-страницу «Настройки» плагина. Найдено и
   исправлено два независимых разрыва, блокировавших lab_admin (роль в
   `lab_members`, отдельная от app-wide роли `lab_permissions`):
   вход на саму страницу «Настройки» (`renderSettings`) был закрыт app-role-
   гейтом (теперь + lab_admin хотя бы одной лабы, `canOpenSettings`/
   `myAdminLabIds`); правка полей конкретной лабы (`PATCH /api/lab/labs/{id}`)
   на бэкенде была буквально `requirePerm("superadmin")` — даже app-admin не
   проходил. Теперь: общие настройки (`lab_id=0`, fallback) — app-admin+;
   настройки конкретной лабы и правка её полей — app-admin+ ИЛИ lab_admin
   именно этой лабы (`requireLabAdminOf`, по образцу уже существующего
   `handleSetLabMember`).
3. **Оповещения стали per-lab** (по прямому запросу пользователя — «у каждой
   лаборатории могут быть свои сроки и свои получатели»): новое поле
   `equipment.lab_id` (nullable — оборудование раньше не было привязано к
   лабе вообще; назначается в форме прибора, селект «Лаборатория»).
   `equipment_notify_settings` теперь по `lab_id` (было — одна строка
   `id=1` на весь модуль); `lab_id=0` зарезервирован под общие/дефолтные
   настройки. Если у лабы нет своих настроек — используются общие (`configured:
   false` в ответе, кнопка «↺ Использовать общие» для отката уже
   переопределённых). Фоновая проверка (`checkEquipmentNotifications`,
   раз в 6ч) резолвит настройки по лабе каждого прибора индивидуально;
   каскад порогов (равенство `daysLeft <= day` для каждого настроенного
   порога) и дедуп писем по (`equipment_id`, `day`, `email`) не менялись.
   Таблица `equipment_notify_settings` ещё не была задеплоена на VDS —
   схема переопределена на месте (`lab_id BIGINT PRIMARY KEY DEFAULT 0`),
   без миграции-переименования.

Backend (`lab-service`, ветка `backend`): `main.go` (роуты `PATCH
/api/lab/labs/{id}`, `GET/POST /api/lab/equipment-notify-settings` —
сниженные базовые гейты + миграция `equipment.lab_id`/`equipment_notify_settings`),
`equipment_notify.go` (полностью переписан — per-lab резолюция), `lims_refs.go`
(`requireAnyLabAdmin`, `Equipment.LabID`, CRUD-поддержка `lab_id`),
`references.go` (`handleUpdateLab` — `requireLabAdminOf`). Клиент:
`lims-view.ts` (фильтры, `canOpenSettings`, перенос раздела оповещений,
раздел в карточке лабы, форма оборудования — селект лабы), `settings-tab.ts`
(раздел оповещений убран), `sync.service.ts`/`types/lims.ts` (сигнатуры
`get/setEquipmentNotifySettings(labId, ...)`, `Equipment.lab_id`).

Живое CDP-тестирование web-паритетных изменений (`sbe-web`, тот же день)
подтвердило отсутствие регрессий в процессе создания заявки — см. его
`AGENTS.md`. Отдельного живого теста на этой сессии для sbe-lims/lab-service
не проводилось (только `go build`/`go vet`/`tsc`/`build`).

`go build ./...`/`go vet ./...` (lab-service) EXIT=0; `npx tsc --noEmit`
EXIT=0; `npm run build` OK. Версия 1.0.1 → **1.0.2b** (суффикс `b` — задета
и сама заявка на правку (плагин), и бэк, ещё не перенесено на `main`).

### 2026-09-03 — v1.0.1b (Оборудование: правки карточки, единая роль, дерево, оповещения о поверке)

Пять замечаний пользователя по модулю «Оборудование» + отдельная правка отображения
дерева. Спека не заводилась — короткая серия точечных UX/модель-фиксов внутри уже
готового модуля (v0.2.11/0.2.12), план держался в `optimized-sleeping-hickey.md`.

- **Карточка не закрывается и не теряет данные** (корень трёх жалоб сразу): раньше
  ЛЮБОЕ действие в карточке (сохранить поля, сохранить/загрузить скан поверки,
  привязать прибор) вызывало `renderEquipment()` — полную перезагрузку списка,
  схлопывающую все карточки и стирающую несохранённые соседние поля. Теперь
  сохранение полей/скана/документов/методов/калибровки точечно обновляет свой блок;
  структурные правки (прикрепить/открепить/удалить) перестраивают список, но нужная
  карточка сама раскрывается на новом месте (`openEquipmentIds` — новое поле вьюхи,
  `renderCollapsibleSection` получил `initialOpen`/`onToggle`).
- **«Основное»/«Вспомогательное» — единая роль на оборудование** (было: отдельная
  роль на каждой связи с методом, `EquipmentMethodLink.role`). Теперь простой чекбокс
  в основных свойствах карточки (`Equipment.type`); для «Вспомогательного» раздел
  «Привязанные методы» скрыт — только связь с другим оборудованием. Backend держит
  старую колонку `method_equipment.role` синхронной с новым `equipment.type` (при
  PATCH и при привязке метода), поэтому `calibration_curve.go` (WP1, калибровочная
  кривая) не переписывался и не должен был сломаться. Осознанная жертва пользователя:
  прежняя гибкость «основное для метода А, вспомогательное для метода Б» у одного и
  того же прибора больше недоступна.
- **Цепочки вспомогательного оборудования разрешены** — раньше вложенность была
  жёстко ограничена одним уровнем. Убран параметр `nested`, рекурсия неограничена;
  бэкенд (`handleAddEquipmentAuxiliary`) отклоняет привязку, которая замкнула бы цикл
  (рекурсивный CTE по `equipment_links`).
- **Список оборудования — как связанные документы в sbe-documents** (отдельная
  правка после первой серии фиксов, по прямому образцу
  `renderCard`/`renderChildItem` из `sbe-documents/src/ui/documents-view.ts`):
  вспомогательное оборудование теперь ВСЕГДА видно вложенным списком под карточкой
  основного прямо в общем перечне (`.tn-lims-equipment-children`), не только при
  раскрытии карточки основного. В отличие от документов (`parent_id` один-к-одному)
  связь оборудование↔оборудование many-to-many — один вспомогательный прибор,
  привязанный к нескольким основным, закономерно рисуется по разу под каждым.
  `renderEquipmentRelated` внутри раскрытой карточки больше не дублирует полную
  карточку — только управление связью (привязать/отвязать).
- **Срок поверки + оповещения**: новое поле «Срок окончания действия»
  (`verification_expiry_date`, у акта поверки — своя дата, без интервала, в отличие
  от `next_calibration`, который сервер считает из интервала калибровки). Новый
  раздел в настройках плагина «Уведомления о сроке поверки» (admin+): включить,
  пороги в днях, получатели (email через запятую) — единые на весь модуль, не по
  каждой единице оборудования (у `Equipment` нет поля-email). Backend — новый
  `equipment_notify.go`, точная калька `documents-service/notify.go` из
  sbe-documents (тикер раз в 6ч, дедуп по (equipment_id, day, email),
  `sendMailWithAttachment` — те же `LAB_MAIL_*`/`LAB_SMTP_*` креды, что и остальная
  почта ЛИМС).

Backend (`lab-service`, ветка `backend`): миграция `equipment.type` (+CHECK,
бэкофилл из старых `method_equipment.role`), `equipment.verification_expiry_date`,
таблицы `equipment_notify_settings`/`equipment_notifications`; новые роуты
`GET/POST /equipment-notify-settings`. `sbe-lims-mobile` не затронут — читает
`equipment/{id}/methods` только GET'ом, контракт не менялся.

- `npx tsc --noEmit` EXIT=0, `npm run build` OK; `go build ./...`/`go vet ./...`
  (lab-service) EXIT=0. Версия 1.0.0 → **1.0.1b** (суффикс `b` — правки ушли на
  `backend`, ещё не перенесены на `main`, см. правило в корневом `plugins/AGENTS.md`).
  Деплой на VDS не выполнялся — только локальная сборка, миграции применятся при
  следующем запуске контейнера `lab`. Живой E2E не проводился — правки объёмные,
  проверка в Obsidian остаётся за пользователем.

### 2026-08-29 — v1.0.0 (WP9: справка в плагине; завершение MVP)

Последний пункт плана MVP — практический гайд по частым сценариям, новый
`src/ui/help.ts` (`LIMS_HELP_MD`/`LIMS_HELP_PATH`) + кнопка «❓ Справка» в
сайдбаре рядом с «⚙ Настройки» — тот же паттерн (создание заметки в вольте при
первом клике, дальше открывается как обычная заметка), что уже был в
`sbe-requests`. Разделы: заявки/канбан, ввод результатов (десктоп vs
мобильный), протоколы, оборудование/калибровка, настройка метода (кратко, для
лаб-админа), права доступа, типовые вопросы. Дашборд не документируется —
удалён из плагина 2026-08-18.

- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.3.5 →
  **1.0.0** (по прямому решению пользователя — отмечает завершение всего плана
  MVP, не обычный минорный шаг). Backend не менялся, деплой не требуется.

### 2026-08-29 — v0.3.5 (WP8: раздел «История» в карточке заявки)

Клиентская часть WP8 (полная история — спека/план/бэкенд в
`lab-service/AGENTS.md`): новый раздел «📜 История» в `renderRequestDetail` —
сворачиваемый, список загружается по клику (`syncService.listAuditLog`), не на
каждый заход в карточку. Рендер строки по `kind`: смена статуса — одна строка
«email, дата — статус: A → B»; результат — «email, дата — серия N (метод M):
создана/изменена» + кнопка «подробнее», раскрывающая полный снимок
`values_before`/`values_after` (список параметр=значение, без diff-подсветки —
YAGNI по спеке). `types/lims.ts` += `AuditLogEntry`.

- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.3.4 →
  **0.3.5**. Backend деплой обязателен (новая таблица `request_audit_log` +
  точки записи), выполнен, health ok, миграция прошла.

### 2026-08-29 — v0.3.4 (адрес служебных писем в настройках лабы)

Клиентская часть находки описанной выше в «Готово» (полная история —
`lab-service/AGENTS.md`): новое поле «Адрес служебных писем (LPITrack)» в
форме лабы (`lims-view.ts`, рядом с чекбоксом автоотправки) — `Lab.
service_email` (`types/lims.ts`) + `syncService.updateLab` (`sync.service.ts`).
Пусто — используется настройка сервера (обратная совместимость).

- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.3.3 →
  **0.3.4**. Backend деплой обязателен (`labs.service_email` + новый триггер
  `triggerProcessingEmail`), выполнен, health ok, миграция прошла.

### 2026-08-29 — v0.3.3 (редактор протоколов: фикс позиции вставки плейсхолдера; защитный фикс невидимого курсора)

Живая жалоба: «в полях ввода не видно курсора, он там не ставится и не
возможно что либо написать, плейсхолдеры вставляются в начало строки, а не
там, куда я их хочу вставить». Оба симптома — про rich-text
contenteditable-поля конфигуратора протокола (`block-editor.ts`,
`renderInlineEditable` / `.tn-lims-rich-line`).

**1. Плейсхолдер вставляется в начало строки — причина найдена и исправлена.**
`insertChipAtCursor` вызывал `editable.focus()` и читал текущее
`window.getSelection()` уже ПОСЛЕ того, как закрывалась
`PlaceholderPickerModal` — модальное окно к этому моменту успевало сбросить
настоящую позицию курсора в поле, так что `focus()` возвращал выделение на
начало содержимого. Фикс: новая `saveSelectionRange(editable): Range | null`
вызывается СРАЗУ при клике на кнопку плейсхолдера (до открытия модалки),
сохранённый `Range` передаётся в `insertChipAtCursor` и восстанавливается
перед вставкой чипа. Это устраняет причину надёжно — логика проверена по
коду, но полноценно (визуально) не тестировалась мной интерактивно.

**2. Невидимый курсор / невозможность печатать — фикс защитный, не
подтверждённый.** Просмотр кода `renderInlineEditable` не выявил структурной
причины (contenteditable выставлен корректно, обработчики не перехватывают
фокус глобально). Единственное найденное расхождение: `.tn-lims-rich-line` в
`styles.css` не задавал явный `color`/`caret-color`, наследуя их от темы
Obsidian — если цвет текста темы близок к белому фону карточки плагина
(`--tn-card`), курсор и текст могли быть практически невидимы. Добавлено
`color: var(--tn-text); caret-color: var(--tn-text);`. **Это не подтверждённая
находка** — нужна проверка пользователем вживую после деплоя.

- `npx tsc --noEmit -p .` EXIT=0, `npm run build` OK. Версия 0.3.2 → **0.3.3**.
  Изменения только клиентские (`block-editor.ts`, `styles.css`) — backend не
  затронут, деплой на сервер не требуется.

### 2026-08-29 — v0.3.2 (кнопка «Пересчитать» на всю заявку; external_id в карточках; фикс легенды графиков)

Три прямых запроса пользователя после живого использования v0.3.1.

**1. «Пересчитать» — одна кнопка на всю заявку, не на каждую серию.**
`renderSeriesList` — кнопка перенесена из цикла по сериям в начало списка
(один вызов `calculateSeries(req.id, series[0].series_num)` — backend и
раньше пересчитывал ВСЕ серии метода заявки при любом вызове, просто UI не
давал вызвать его один раз на заявку). Заодно фикс: после пересчёта
перерисовывается ВСЯ карточка заявки (`renderRequestDetail`), не только
список серий — раньше пересчитанные агрегированные/классификационные
значения были видны только в «Кратком виде» ниже, который отдельным вызовом
не обновлялся (жалоба «нужно выйти из заявки и зайти заново»).

**2. `external_id` в скобках рядом с номером заявки.** Новый хелпер
`requestNumberLabel(req)` — применён в списках заявок (`renderRequests`,
Kanban-карточка) и в заголовке карточки заявки (`renderRequestDetail`).
Тот же паттерн перенесён и в `sbe-requests` (см. AGENTS.md того плагина) —
там заменил отдельную строку «Внешний идентификатор» (та же информация,
компактнее).

**3. Легенда графиков с несколькими сериями — фикс на бэкенде
(`lab-service/charts.go`).** `buildChartSeriesFromTimeseries` раньше давал
ОДНУ И ТУ ЖЕ подпись кривой для каждой серии эксперимента (если у заявки
несколько серий с сырой кривой прибора — все кривые на графике неотличимы).
Теперь при >1 серии подпись дополняется номером серии
(`"Канал 1 (серия 2)"`); при ровно одной серии — без изменений. Подробности,
тесты, живая проверка — `lab-service/AGENTS.md`.

- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.3.1 →
  **0.3.2**. Backend деплой обязателен (`charts.go`/`results.go`/
  `protocol.go`), выполнен, health ok.

### 2026-08-29 — v0.3.1 (фикс — вернулась кнопка «Пересчитать» серии)

Живая жалоба: «куда делась кнопка "Рассчитать" со страницы заявки». Найдено —
старая кнопка «🔄 Рассчитать» (считала ТОЛЬКО первую серию заявки) потерялась
при переписывании этого экрана под WP3a (`git 75bc592`) — тогда казалось
избыточной, т.к. `saveResultSeries` и так пересчитывает формулы на каждое
сохранение серии. Не покрытый случай: формулу метода поправили/добавили
ПОСЛЕ того, как серия уже сохранена — нужен способ пересчитать существующую
серию БЕЗ повторного ввода значений (конкретный повод — метод ГГ, формула
"степень повреждений по длине" не была введена в конфигураторе, серии
сохранялись без неё; пользователь сам нашёл причину непосчитанного значения,
но кнопки для пересчёта задним числом не оказалось).

- `lims-view.ts` (`renderSeriesList`): новая кнопка «🔄 Пересчитать» в ряду
  каждой серии (тот же паттерн, что ✏️ Править / 🗑) — вызывает уже
  существующий `syncService.calculateSeries(req.id, s.series_num)`
  (backend не менялся, эндпоинт никуда не девался, просто не был вызван
  из UI). В отличие от старой версии — per-series, не только первая серия
  заявки (backend и раньше пересчитывал ВСЕ серии метода при вызове, просто
  клиент не давал выбрать, для какой серии нажать).
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.3.0 →
  **0.3.1**. Backend не менялся — деплой не требуется.

### 2026-08-29 — v0.3.0 (проект по умолчанию для писем без ЕКН)

Прямой запрос пользователя после включения приёма почты (WP7): «ЕКН как
проект» уже было основным правилом (`ensureEknProject`), но заявки БЕЗ ЕКН в
письме оставались без проекта вовсе — добавлен настраиваемый проект по
умолчанию именно для этого случая (правило «ЕКН как проект» приоритетнее,
не меняется).

- `settings-tab.ts`: новое поле «Проект по умолчанию (без ЕКН)» в разделе
  «Приём результатов по email» — тот же канал (`LAB_MAIL_ENV_KEYS` += 
  `LAB_MAIL_DEFAULT_PROJECT_CODE`), что остальные почтовые настройки.
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.29 →
  **0.3.0** (по договорённости с пользователем — после 0.2.29 следующая
  версия 0.3.0, не 0.2.30). Backend менялся (`email_ingest.go`/`requests.go` —
  ветка `backend`; `sbe-core/auth-service/env_admin.go` — whitelist) — полная
  запись, живая миграция 13 существующих заявок в проект `EML` —
  `lab-service/AGENTS.md`.

### 2026-08-29 — v0.2.29 (WP6 — динамическая нумерация заголовков, путь б)

Роадмап: `docs/superpowers/plans/2026-08-27-mvp-roadmap-plan.md`, WP6 — развилка
путей (a)/(b) решена пользователем: **путь (б)** — плейсхолдер номера внутри
вручную набранного текста, а не авто-печать `block.title`. Причина выбора не
только предпочтение — путь (a) поменял бы вывод УЖЕ настроенных, реально
уходящих заказчикам протоколов (ГГ/ГВ/РП и т.д.) в момент деплоя без их ведома;
путь (б) органически opt-in — ничего не меняется, пока админ сам не вставит
плейсхолдер.

- `types/lims.ts`: `PlaceholderSource` += `'heading_number'`.
- `block-editor.ts`: `PlaceholderPickerModal` — новый раздел «Нумерация» с
  пунктом «№ п/п (по порядку среди блоков с этим плейсхолдером)»;
  `resolvePlaceholderLabel` — чип показывает «№ п/п». Доступно из ЛЮБОГО
  инлайн-редактируемого места (заголовок/абзац/пункт списка/ячейка
  static_table) — не только заголовков буквально, без искусственного
  ограничения (плейсхолдер работает одинаково везде).
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.28 →
  **0.2.29**. Backend менялся (`protocol.go`) — полная запись и живой E2E
  (номер блока пропускает блоки БЕЗ плейсхолдера, не сбивая счёт следующих) —
  `lab-service/AGENTS.md`.

### 2026-08-29 — v0.2.28 (WP4 — created_by/updated_by серий результатов)

Роадмап: `docs/superpowers/plans/2026-08-27-mvp-roadmap-plan.md`, WP4.
Решение пользователя по правам уже снято с повестки раньше («редактировать
могут все испытатели и все администраторы лаборатории», строгий gating не в
MVP) — реальный UI редактирования серии уже есть (`renderSeriesEditForm`,
WP3a). Оставался только сам факт фиксации «кто/когда менял» — база для
будущего WP8 (журнал изменений), по тому же паттерну, что уже есть у
`equipment_calibrations.created_by`.

- `types/lims.ts`: `MeasurementResult` += `created_by`/`updated_by: string`.
- `lims-view.ts` (`renderSeriesList`): под названием серии — `изменено:
  <email>`, если `updated_by` не пусто (пусто у серий, созданных до этой
  доработки, и у авто-статистической строки — она не создаётся через
  `saveResultSeries`, см. ниже).
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.27 →
  **0.2.28**. Backend менялся (новые колонки + сигнатура `saveResultSeries`)
  — полная запись и живой E2E (created_by сохраняется при создании, НЕ
  меняется при повторном сохранении другим пользователем, updated_by
  меняется) — `lab-service/AGENTS.md`.

### 2026-08-29 — v0.2.27 (WP5 — «№ серии» → «№ п/п» для динамических таблиц)

Роадмап: `docs/superpowers/plans/2026-08-27-mvp-roadmap-plan.md`, WP5 —
исходный запрос MVP-документа «вставка динамических таблиц основного и
вспомогательного оборудования — добавить возможность заменять № серии на
№ п/п». По факту кода (`TableColumn` рендерит строку таблицы `i+1` — обычная
позиция строки, не привязана к «серии» содержательно) — минимальная реализация:
новый `TableColumn.kind="sequential"`, рендерится ИДЕНТИЧНО `series_no` (то же
`i+1`), только другая дефолтная подпись «№ п/п» вместо «Серия». Отдельный
`kind`, а не просто другой `Label` у `series_no` (это уже было возможно и
раньше, см. `TestRenderTableSeriesNoColumn`) — так пресет виден в списке
добавления колонки без необходимости вручную набирать текст подписи; полезно
для ЛЮБОЙ таблицы, где «Серия» не подходит по смыслу (не только оборудование).

- `lab-service/results.go`: `TableColumn.Kind` doc-comment расширен.
- `lab-service/protocol.go`: `tableColumnHeader` += ветка `"sequential"` →
  «№ п/п» (или явный `Label`); обе точки рендера таблицы (`renderTableHTML`/
  `renderTableDocx`) — условие `c.Kind == "series_no" || c.Kind == "sequential"`
  вместо только `"series_no"`.
- `src/types/lims.ts`: `TableColumn.kind` += `'sequential'`.
- `src/ui/block-editor.ts` (`renderTableNodeEditor`): новый пункт «№ п/п
  (порядковый номер)» в списке добавления колонки (тот же паттерн, что
  `series_no`/`photo_before`/`photo_after` — не добавляется повторно, если уже
  есть в таблице); отображение в списке колонок + подсказка дефолтной подписи.
- Тест (`protocol_test.go`): `TestRenderTableSequentialColumn` — дефолтный
  заголовок «№ п/п», нумерация 1..N, явный `Label` переопределяет заголовок
  (по образцу уже существующего `TestRenderTableSeriesNoColumn`).
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK; `gofmt`/
  `go build`/`go vet`/`go test` — чисто. Версия 0.2.26 → **0.2.27**.
- **Живой E2E** (throwaway-метод id=10 с колонкой `sequential` + обычным
  атрибутом, throwaway-заявка id=1421 с 2 сериями, оба удалены после
  проверки): протокол показал заголовок `"№ п/п"` и строки `1`/`2` —
  корректно.
- Backend — деплой обязателен (изменения в `protocol.go`); выполнен, `docker
  compose up -d --build lab`, health ok.
- **Не закоммичено** — ждёт «фиксируй»/«коммить и пуш» (per поточное указание
  пользователя «сейчас проверить не могу, позже все проверю»).

### 2026-08-29 — v0.2.26 (редизайн таймера: список кнопок; рекомендуемые значения)

Живая жалоба пользователя после тестирования v0.2.25 (полный текст и разбор —
`lab-service/AGENTS.md`, раздел «Редизайн: список кнопок вместо одной
фиксированной»): (1) единственная жёстко названная кнопка «Зафиксировать
событие» не годится — разным событиям нужны разные названия и разный
результат; (2) непонятно, как задать «рекомендуемые значения» для текстовых
полей (испытатель должен точно набирать стандартные формулировки вручную);
(3) формат лога должен быть «N сек - label», а не «label (Nс)».

- `types/lims.ts`: `MethodOperatorForm.timer` — было `{capture?, log?}`, стало
  `{buttons: Array<{label, action: {kind:'capture',booleanFieldId,
  secondsFieldId} | {kind:'log',attributeId}}>}`. `OperatorFormField` +=
  `suggestions?: string[]` (кнопки-подсказки под текстовым полем — клик
  подставляет точный текст, поле остаётся свободным вводом, НЕ select).
- `lims-view.ts`:
  - `renderTimerWidgetDesktop` переписан на цикл по `timer.buttons` —
    диспетчер по `action.kind` вместо двух отдельных `if (timer.capture)`/
    `if (timer.log)` блоков; лог-превью общий на все log-кнопки (по
    уникальным `attributeId`), формат `"${seconds} сек - ${label}"`.
  - Редактор «Таймер» в конфигураторе — был 2 отдельных чекбокса
    (захват/лог), стал единый список кнопок: подпись + select вида действия
    + условные selects атрибутов (2 для capture, 1 для log), добавление/
    удаление строк.
  - `renderDesktopResultField` — рендерит `suggestions` как кнопки-чипсы под
    полем (только для обычного текстового `<input>`, не select/boolean/
    curve/event_log/photo).
  - `renderOperatorFormRows` — новая кнопка-тоггл «💡 рекомендуемые значения»
    (только для `data_type=text`) + `toggleSuggestionsPanel` (тот же
    flat-список-с-добавлением паттерн, что `renderSelectOptionsEditor`, но
    над `OperatorFormField.suggestions`).
  - Обе точки инициализации/рендера `timer` защищены `Array.isArray(...
    .buttons)` — старая форма `{capture,log}` (если где-то не мигрирована)
    не роняет форму `TypeError`, просто не показывает таймер.
- **Найден и исправлен реальный баг, живший в проде** (тот же класс, что уже
  чинили у `Timer` в v0.2.25, но на этот раз подтверждено ЖИВЫМ GET на
  реальном методе «ГВ»): `OperatorFormField` (Go, `lab-service/results.go`) не
  имел полей под `default`/`visibility` (WP3c ч.1) — оба молча терялись при
  любом `GET /methods`. Исправлено там же, тем же паттерном
  (`json.RawMessage`, opaque). Подробности и живая проверка —
  `lab-service/AGENTS.md`.
- **Миграция реального метода «ГВ»** (id=2, единственный в проде с настроенным
  `timer`) — старая форма `{capture,log}` переведена в `{buttons:[...]}` через
  `PATCH /methods/2`, поведение сохранено (см. `lab-service/AGENTS.md` за
  точным payload).
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.25 →
  **0.2.26**.
- **Живой E2E** (throwaway-метод+заявка, удалены после проверки; плюс живая
  миграция реального метода «ГВ», не throwaway) — подробности в
  `lab-service/AGENTS.md`.

### 2026-08-28 — v0.2.25 (WP3c ч.2/2 — таймер, захват события, лог наблюдений; WP3 закрыт)

Спека/план: `docs/superpowers/specs/2026-08-28-sbe-lims-timer-widget-design.md`
+ `-plan.md`. Последняя часть WP3 — весь пакет теперь готов. Подробности
(находки брейнсторма, 2 реальных бага — латентный Agg-баг для нечисловых
одиночных плейсхолдеров, потеря `timer` при round-trip `operator_form`) —
`lab-service/AGENTS.md`.

- `types/lims.ts`: `AttributeDataType` += `'event_log'`; `MethodOperatorForm`
  += `timer?: { capture?: {booleanFieldId, secondsFieldId}, log?:
  {attributeId, events} }` — фиксированная схема, не общий расширяемый
  механизм виджетов.
- `lims-view.ts`: новый `renderTimerWidgetDesktop` (портированный аналог
  мобильного) — общий секундомер, кнопка «Зафиксировать событие» (пишет
  `'Да'`/секунды в 2 обычных поля, ОСТАНАВЛИВАЕТ таймер — прямая
  формулировка роадмапа), кнопки лога (копят `{label,seconds}` в
  event_log-атрибут, НЕ останавливают таймер). Состояние таймера — только
  рендер формы, не сохраняется. Конфигуратор: `renderAttributeRows` += тип
  «Лог наблюдений»; новый блок «Таймер» в редакторе «Форма для испытателя» —
  чекбоксы включения захвата/лога, select атрибутов (отфильтрованы по
  подходящему `data_type`), список названий кнопок лога (тот же list-editor
  паттерн, что опции select-атрибута WP3c ч.1, не переиспользует ту же
  функцию напрямую — сигнатура завязана на `MethodAttribute`).
- `block-editor.ts`: `renderAggChoice` — параметр `photoOnly` → `seriesOnly`,
  теперь ограничивает выбор агрегации до first/last и для `event_log` тоже
  (нашли баг: без этого нечисловой одиночный плейсхолдер резолвится в
  пустоту, см. `lab-service/AGENTS.md`).
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.24 →
  **0.2.25**.
- **Живой E2E** (throwaway-метод+заявка, удалены после проверки) —
  подробности в `lab-service/AGENTS.md`.

### 2026-08-28 — v0.2.24 (WP3c часть 1/2 — select-виджет, дефолты, условная видимость)

Спека/план: `docs/superpowers/specs/2026-08-28-sbe-lims-select-defaults-visibility-design.md`
+ `-plan.md`. Первая часть WP3c (по решению пользователя разбит на 2
под-проекта — вторая, виджеты-триггеры типа секундомера, ещё не начата).
Найдено при брейнсторме: "Да/Нет" рендерился как обычный текстовый `<input>`,
"выбор из списка" как тип данных не существовал вовсе — без constrained-choice
виджета дефолты/условная видимость опирались бы на нежёстко заданный
свободный текст. Backend НЕ менялся — чисто клиентская конфигурация (JSONB,
не парсится сервером структурно) и клиентский рендер.

- `types/lims.ts`: `AttributeDataType` += `'select'`; `MethodAttribute` +=
  `options?: string[]`; `OperatorFormField` += `default?` (`{kind:'literal',
  value}` или `{kind:'today'}` — без DSL, единственный реальный пример был
  "дата = сегодня") и `visibility?` (`{logic:'and'|'or', conditions:
  Array<{field, operator, value}>}` — плоский список, ОДИН связыватель на всё
  поле, переиспользует уже существующий `ComparisonOperator`).
- `renderDesktopResultField` (форма правки серии) — новая ветка
  `select`/`boolean` (`<select>`, пустой плейсхолдер-опция); применение
  дефолта один раз при рендере (не перетирает уже сохранённое); новые
  `compareFieldCondition`/`updateFieldVisibilityDesktop`/
  `todayLocalDateStringDesktop` — **не переиспользуют DSL/классификацию**
  (те выполняются только на сервере, Go) — маленькие новые клиентские чистые
  функции, специально для живого пересчёта видимости при каждом изменении
  формы. `renderSeriesEditForm` — живой пересчёт видимости на input/change,
  скрытые поля исключаются из отправляемых `values` на submit.
- Конфигуратор: `renderAttributeRows` += тип "Выбор из списка" + новый
  `renderSelectOptionsEditor` (add/remove вариантов, показывается только при
  `data_type==='select'`); `renderOperatorFormRows` += блок дефолта
  (select/boolean — выбор варианта; date — чекбокс «по умолчанию сегодня») и
  новый `toggleVisibilityPanel` (тот же toggle-по-кнопке паттерн, что у
  синонимов атрибута) — И/ИЛИ + список условий (поле → оператор → значение),
  поле условия — только ДРУГИЕ поля этой же формы.
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.23 →
  **0.2.24**.
- Верификация: только tsc/build + код-ревью (backend не менялся, риска для
  данных нет — фича чисто про клиентский рендер); живой E2E не требовался
  (нет серверной логики, которую стоило бы проверять живым запросом).

### 2026-08-28 — v0.2.23 (фикс: колонки «Фото до/после испытания» в таблице результатов)

Живая жалоба на заявке 287/2026: фото, загруженное через мобильный пикер «Фото
до/после испытания», показывалось как обычное вложение заявки в «Заявки», но не
как изображение в таблице «📊 Результаты испытания». Причина и бэкенд-фикс —
`lab-service/AGENTS.md`, «⚠️ Найден и исправлен реальный баг: фото «до/после»
из мобильного пикера не показывалось в таблице результатов».

- `block-editor.ts` (`renderTableNodeEditor`) — две новые фиксированные опции в
  селекторе колонок таблицы результатов: «📷 Фото до испытания»/«📷 Фото после
  испытания» (`kind: 'photo_before'`/`'photo_after'`), рядом с уже существующей
  «Серия (номер по порядку)». Читают top-level `measurement_results.photo_before`/
  `photo_after` — НЕ атрибут метода, поэтому не нужно ничего настраивать в
  input_parameters/operator_form, колонка добавляется в любую таблицу как есть.
- `types/lims.ts`: `TableColumn.kind` += `'photo_before' | 'photo_after'`.
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.22 →
  **0.2.23**.
- **Живой E2E на реальной заявке 287/2026** (подтверждено пользователем) — см.
  подробности в `lab-service/AGENTS.md`.

### 2026-08-28 — v0.2.22 (WP3a — навигация по сериям (десктоп: просмотр/правка) + фикс графика калибровки + КРИТИЧНО: фикс потери второй+ серии)

Третий пакет основного плана MVP (см.
`docs/superpowers/plans/2026-08-28-sbe-lims-series-navigation-plan.md`). Создание
серий теперь ТОЛЬКО с мобильного — десктоп получил просмотр/правку/удаление уже
введённых данных вместо крудой формы «параметр=значение».

- **Живая жалоба пользователя, попутно исправлена**: журнал калибровок показывал
  `[object Object]` для калибровочного атрибута-«кривой» (WP1) и не давал
  посмотреть график, хотя серверный PNG-эндпоинт уже работал. Новый
  `getCalibrationCurveChartDataUrl` (`sync.service.ts`) + `ImageModal` — кнопка
  «📈» в журнале калибровок теперь открывает настоящий график. Ячейка
  «Параметры» перестала пытаться `String()`-ить массив точек.
- **`renderRequestDetail`**: крудая форма добавления серии убрана из UI (код
  `saveResult`/эндпоинт НЕ удалялись — можно будет вернуть кнопку добавления
  позже, решение пользователя). Новый `renderSeriesList` — список серий с
  «✏️ Править»/«🗑 Удалить» на каждой; `renderSeriesEditForm` — портированный
  аналог мобильного `renderFormField` (диспетчер по `AttributeDataType`,
  предзаполнение текущими значениями серии, тот же принцип дублирования кода
  между репозиториями, что и везде в проекте) + селектор оборудования, если у
  метода несколько «Основных» единиц. `sync.service.ts` — новый
  `deleteResultSeries`.
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.21 →
  **0.2.22**.
- **⚠️ КРИТИЧНАЯ находка живым E2E, backend, не специфично для WP3a** (см.
  `lab-service/AGENTS.md`, раздел «Найден и исправлен реальный баг»): вторая и
  последующие серии заявки могли молча теряться из-за коллизии номера строки
  статистики с номером следующей настоящей серии — воспроизведено и
  исправлено. Затрагивало старую кнопку «Добавить серию» (только что убранную
  из UI) и мобильный сценарий добавления серии; email-путь не затронут.
  **Прошлые потери (если случались) невосстановимы** — подробности и точный
  механизм в `lab-service/AGENTS.md`.
- Живой E2E на VDS (тестовые заявка/объект, метод РП, лаборатория ЛПИ, удалены
  после проверки): 3 серии без явного номера → все 3 сохранились (было — баг
  выше); удаление средней серии → перенумерация оставшихся; правка на месте
  без создания новой строки; удаление несуществующей серии → 404.

### 2026-08-28 — v0.2.21 (WP2 — исходящая почта: результаты заказчику + дубль в LPITrack)

Второй пакет основного плана MVP (см.
`docs/superpowers/plans/2026-08-28-sbe-lims-outbound-email-plan.md`). `lab-service`
раньше умел только принимать почту — теперь и отправляет: результаты испытания
заказчику + дубль на ящик легаси-трекера LPITrack, автоматически при завершении
заявки (по лабе — включается отдельно) и/или вручную кнопкой. Backend-подробности,
включая найденный и исправленный инфраструктурный баг с `host.docker.internal` —
`lab-service/AGENTS.md`.

- `types/lims.ts`: `Lab` += `auto_send_email`; новый `SentEmail`.
- `sync.service.ts`: `updateLab` += `auto_send_email`; новые
  `sendRequestEmail(requestId)`/`listSentEmails(requestId)`.
- `lims-view.ts`: `renderLabForm` — чекбокс «Автоматически отправлять письмо
  заказчику при завершении заявки» (только при редактировании существующей лабы).
  Карточка заявки (`renderRequestDetail`) — кнопка «✉ Отправить письмо» (доступна
  всегда, не завязана на автонастройку/статус) + список журнала отправок под ней.
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.20 →
  **0.2.21**.
- **Живой E2E на VDS** (подробности `lab-service/AGENTS.md`): автоотправка при
  завершении (оба письма реально ушли), защита от повторной автоотправки при
  повторных сменах статуса, ручная кнопка шлёт независимо от журнала, заявка без
  external_id — только заказчику, лаба с выключенной автоотправкой — вообще без
  авто-писем. Тестовые лаба/метод/объект/заявки удалены после проверки.
- Не закоммичено — ждёт «фиксируй»/«коммить и пуш».

### 2026-08-28 — v0.2.20 (WP1 — калибровочная кривая метода РП: точки x→y + селектор оборудования)

Первый пакет основного плана MVP (см. `docs/superpowers/plans/2026-08-28-sbe-lims-calibration-curve-plan.md`,
брейнсторм → спека → план → реализация в этой же сессии). Метод РП фиксирует
НЕСКОЛЬКО калибровочных точек (расстояние→тепловой поток) вместо одного числа;
DSL-функция `interpolate(x,xs,ys)` уже была готова в `lab-service` — не хватало
только данных для неё (backend-подробности — `lab-service/AGENTS.md`).

- `types/lims.ts`: `AttributeDataType` += `'curve'` (только для
  `calibration_attributes` — набор точек x→y, не одно число);
  `MeasurementResult` += `equipment_id`; новый `MethodEquipmentLink` (вся
  таблица `method_equipment` одним запросом).
- `lims-view.ts`: конфигуратор — `data_type` селектор калибровочного атрибута
  получил опцию «Кривая (точки x→y)»; форма записи калибровки
  (`renderEquipmentCalibrationForm`) — новый `renderCurvePointsField` (таблица
  строк x/y с добавлением/удалением) вместо одного `<input>` для таких
  атрибутов. Форма ввода результата испытания (крудая `parseValues`-форма) —
  новый `<select>` «Оборудование», только когда у метода заявки БОЛЬШЕ ОДНОЙ
  единицы «Основного» оборудования (`listAllMethodEquipment`+`listEquipment`);
  при ровно одной единице сервер резолвит её сам, поле не показывается.
- `sync.service.ts`: `saveResult` += `equipment_id`; новый
  `listAllMethodEquipment()`.
- `npx tsc --noEmit -skipLibCheck` EXIT=0, `npm run build` OK. Версия 0.2.19 →
  **0.2.20**.
- **Живой E2E на VDS** (тестовые equipment/calibrations/request, удалены после
  проверки, метод РП восстановлен к исходному конфигу — подробности
  `lab-service/AGENTS.md`): 2 тестовые установки с разными калибровочными
  кривыми → один и тот же вход → разные, верно посчитанные результаты (150 и
  175, совпало с ручным расчётом); PNG-график кривой корректен; понятная
  ошибка при неоднозначном оборудовании; отказ при оборудовании, не
  являющемся «Основным» метода.
- **`sbe-lims-mobile` (v0.1.5 → 0.1.6, отдельная запись в его AGENTS.md)** —
  тот же виджет точек x→y (мобильный вариант) в форме калибровки + тот же
  `<select>` «Оборудование» в форме результатов; собранный `main.js`
  скопирован в vault-плагин.
- Не закоммичено — ждёт «фиксируй»/«коммить и пуш».

### 2026-08-28 — v0.2.19 (фикс: системные поля формы испытателя пропадали при сохранении конфигуратора)

Живая жалоба пользователя: добавил системные поля (`report_date`/`samples_in_date`/
`exp_date`/`amb_temp`/`amb_pres`/`amb_moist`) в «Форму для испытателя» через
конфигуратор метода — визуально добавлялись, но после «Сохранить» пропадали
(перепроверено на реальном методе, ошибка не показывалась). Причина —
`lims-view.ts` `buildPatch()`: `operator_form: { fields: operatorFormFields
.filter(f => attrIds.has(f.attribute_id)) }` — `attrIds` содержал только id
реальных атрибутов метода, хотя UI (кнопка «➕ Поле формы») явно позволяет
добавлять и системные поля (`OPERATOR_FORM_SYSTEM_FIELDS`) — они молча
вырезались перед отправкой PATCH, поэтому запрос уходил «чистым» и ошибки не
было. Фикс: `allowedFieldIds` теперь объединяет `attrIds` и id из
`OPERATOR_FORM_SYSTEM_FIELDS` (та же логика, что уже использовалась при
открытии конфигуратора, строка ~1662, — на save её просто не применили).
Проверено живьём: пересобран main.js, пользователь добавил поля заново и
сохранил, подтверждено в БД VDS (`methods.operator_form` метода ГГ теперь
содержит `samples_in_date`/`exp_date`/`amb_pres`/`amb_moist`).

### 2026-08-27 — v0.2.18 (принцип «ничего не настраиваемого в выводе»: метод/системные поля — только через конфигуратор; копирование блоков между методами; бэкенд принимает системные поля результатов)

По прямому требованию пользователя: «в протоколы/выписки/UI не должно попадать
ничего, что не может быть настроено пользователем в конфигураторе» — обнаружено
после жалобы «в форме испытателя не выводятся влажность/давление/температура,
хотя написано, что подставятся сами». Разбор показал: жёсткий заголовок
протокола/выписки/короткого вида (`"Метод: ..."` + таблица Наименование/Объект/
ЕКН/Заказчик/Статус/Создана) печатался Go-кодом в обход блочной системы, а
«Форма для испытателя» отбрасывала любое поле, чей `attribute_id` не был
атрибутом метода — из-за этого системные поля (даты/условия среды) физически
нельзя было добавить, даже вручную поправив JSON.

- **lab-service** (protocol.go, results.go — первый реальный деплой бэкенда за
  весь QR-проект):
  - Убраны хардкод-заголовок и `<h2>Метод: %s</h2>` из `protocolHTML`/
    `protocolDocx` — БЕЗ миграции (прямое решение пользователя): у существующих
    методов эти строки просто пропадут из вывода, пока админ не добавит
    соответствующие блоки заново (см. ниже — копирование между методами этому
    помогает).
  - Новый плейсхолдер `method_name` (`placeholderCtx.methodName`,
    `resolveSystemPlaceholder`) — теперь обычный плейсхолдер, как и остальные.
  - `POST /requests/{id}/results` принимает `report_date`/`samples_in_date`/
    `exp_date`/`amb_temp`/`amb_pres`/`amb_moist` (новая `updateRequestSystemFields`,
    `COALESCE(NULLIF($x,''),col)` — пустая строка не затирает уже пришедшее из
    письма). Раньше эти колонки `requests` писал ТОЛЬКО email_ingest.go — прямого
    API-пути не было вообще, форма была не более чем витриной.
  - `go build`/`go vet`/`go test ./...` — чисто. **Задеплоено на VDS**
    (`docker compose up -d --build lab`), health `{"status":"ok"}`. E2E: живой
    `POST .../protocol?kind=ui` на заявке 1407 подтвердил — хардкод-заголовка в
    ответе больше нет, документ начинается сразу с содержимого блоков. Приём
    системных полей результатов отдельно вживую не гонялся (тот же принцип, что
    и раньше — не писать в реальную заявку ради теста; код прошёл `go test`,
    механизм идентичен уже проверенному calibration/photo_before пути).
  - Отдельно НЕ трогали: те же 3 системных поля калибровки (`amb_temp`/
    `amb_pres`/`amb_moist` у `POST /equipment/{id}/calibrations`) — там сервер
    и раньше принимал их отдельными полями формы для ЛЮБОЙ калибровки, без
    привязки к схеме, это осознанно оставлено как есть (не «опционально
    настраиваемое», а часть журнала калибровки).
- **block-editor.ts**: `SYSTEM_PLACEHOLDERS` — добавлен `method_name`; у полей,
  которые реально вводит испытатель (даты, температура/давление/влажность),
  теперь есть `data_type` — по нему построен новый `OPERATOR_FORM_SYSTEM_FIELDS`
  (подмножество, годное для «Формы для испытателя»; реквизиты заявки/объекта
  туда не попадают — их испытатель не вводит).
- **lims-view.ts** — «Форма для испытателя» (и обычная, и калибровки — фильтр
  один и тот же принцип):
  - Фильтр `operator_form.fields` при загрузке конфига перестал отбрасывать
    системные поля.
  - «➕ Поле формы» — раньше молча добавляло первый свободный атрибут метода,
    теперь открывает `Menu` (атрибуты метода + отдельным разделом системные) —
    пользователь явно выбирает, что добавить.
  - `<select>` смены поля в строке — тоже показывает оба списка (`<optgroup>`).
  - Предпросмотр «как увидит испытатель»: убрано «заполняются автоматически,
    настраивать не нужно» (это больше не так) — справочно показывает только
    реквизиты заявки (`data_type` не задан), которые ДЕЙСТВИТЕЛЬНО нельзя
    добавить как поле ввода.
  - Новая кнопка «📋 Скопировать блок из другого метода» в разделе
    «Представление данных» — двухуровневое `Menu` (метод → блок), глубокая
    копия блока с новым `id`. Прямое следствие решения «без миграции»: раньше
    заголовок был у всех методов бесплатно, теперь его можно быстро
    растиражировать копированием вместо ручного набора текста в каждом методе.
- Пикер «Вставить плейсхолдер» в редакторе блоков представления уже перебирал
  весь `SYSTEM_PLACEHOLDERS` — новый `method_name` появился там без отдельных
  правок (пункт C плана «дать вставлять системные плейсхолдеры через пикер»
  оказался уже реализован).
- **sbe-lims-mobile** (v0.1.5, отдельная запись в его AGENTS.md) — форма
  результатов теперь различает системные поля (по `OPERATOR_FORM_SYSTEM_FIELDS`
  наравне с атрибутами метода) и шлёт их отдельно от `values`; форма калибровки
  всегда показывает температуру/давление/влажность (сервер их и так принимает
  для любой калибровки).

`npx tsc --noEmit` EXIT=0, `npm run build` OK. Версия 0.2.17 → **0.2.18**.

### 2026-08-27 — v0.2.17 (QR — простой номер/код, не ссылка на плагин)

Тот же фикс и по той же причине, что в sbe-requests v0.2.3 (см. его AGENTS.md
за подробностями — у Obsidian mobile нет доступа к камере, диплинк и
встроенный сканер в sbe-lims-mobile не заработали). `buildMobileDeepLink`
убран из `mobile-link.ts`; QR в детали заявки теперь кодирует
`fullRequestNumber(req)`, QR оборудования — `eq.code`/`#id` (тот же текст, что
и подписи под ними, в т.ч. на печатной этикетке).

`npx tsc --noEmit` EXIT=0, `npm run build` OK. Версия 0.2.16 → **0.2.17**.

### 2026-08-27 — v0.2.16 (фикс: QR-этикетка оборудования была не видна без привязки к методу)

Найдено пользователем: QR-этикетка оборудования жила внутри
`renderEquipmentCalibrationBlock`, который рендерится только при
`methodLinks.some(l => l.role === 'main')` — т.е. этикетку невозможно было
получить/распечатать, пока оборудование не привязано к методу как «Основное».
Это неверный порядок: этикетку нужно клеить на прибор сразу, до всякой
настройки метода. Блок QR перенесён в `renderEquipmentCardDetails` — теперь
показывается всегда, сразу после основных полей карточки, независимо от
привязки к методам.

`npx tsc --noEmit` EXIT=0, `npm run build` OK. Версия 0.2.15 → **0.2.16**.

### 2026-08-27 — v0.2.15 (фикс: печать QR-этикетки оборудования давала пустой документ)

Тот же баг и тот же фикс, что в sbe-requests v0.2.2 (см. его AGENTS.md за
подробным объяснением причины): `window.print()` поверх окна Obsidian
перехватывался чем-то во внутренней print-логике самого Obsidian, оставляя
пустую страницу. `printEquipmentQrLabel` переписан на самодостаточный
HTML-файл (`yourbase/sbe_lims/print/`), открываемый системным браузером через
`shell.openPath` (новый общий `openPrintableHtml`, тот же паттерн, что уже
используется в проекте для скачанных файлов). Убран мёртвый CSS
(`.tn-lims-qr-sheet`/`@media print`).

`npx tsc --noEmit` EXIT=0, `npm run build` OK. Версия 0.2.14 → **0.2.15**.

### 2026-08-27 — v0.2.14 (QR в заявке + QR-этикетка оборудования → мобильный ввод)

Часть 2 общего проекта «QR → мобильный ввод результатов» (спека
`docs/superpowers/specs/2026-08-27-sbe-lims-mobile-qr-design.md`, план
`docs/superpowers/plans/2026-08-27-sbe-lims-mobile-qr-plan.md`) — показ QR со
стороны лаборатории, см. также sbe-requests (генерация/печать при регистрации
заявки) и новый плагин `sbe-lims-mobile` (сканирование/ввод, без изменений
самого lab-service).

- Новый `src/utils/mobile-link.ts` (тот же маленький хелпер, что в
  sbe-requests — намеренно не общий пакет, см. спеку «Границы»):
  `buildMobileDeepLink`/`renderMobileQr`/`mobileQrDataUrl` на `qrcode`.
- Деталь заявки (`renderRequestDetail`): блок QR — тот же диплинк, что печатает
  sbe-requests, на случай переклейки/повторной печати одной этикетки.
- Карточка оборудования (`renderEquipmentCalibrationBlock`): постоянная
  QR-этикетка прибора (`action=calibrate&equipment=<id>`) + кнопка
  «Калибровать (печать этикетки)» — печатает ту же самую этикетку через
  `printEquipmentQrLabel` (`window.print()` по `.tn-lims-qr-sheet`,
  `@media print`), ничего не создаёт на сервере — сканирование на мобильном
  каждый раз открывает форму НОВОЙ записи журнала калибровки.

`npx tsc --noEmit` EXIT=0, `npm run build` OK. Версия 0.2.13 → **0.2.14**.

### 2026-08-26 — v0.2.13 (ИИ-помощник подбора id для атрибутов калибровки)

Прямой запрос пользователя, продолжение v0.2.12 в той же сессии. Значок **🤖** рядом
с полем «Название» у каждого атрибута калибровки (`renderCalibrationAttributeRows`) —
точное зеркало уже существующей кнопки у обычных атрибутов метода
(`suggestAttributeId`, v0.1.16): по введённому названию ищет среди уже существующих
атрибутов КАЛИБРОВКИ (этого и других методов — новый
`collectExistingCalibrationAttributeSummaries`, отдельный список, не путается с
обычными измеряемыми показателями метода) смыслово подходящий и копирует id, либо
просит ИИ предложить новый (перевод смысла на английский). Новый
`suggestCalibrationAttributeId` переиспользует уже существующий серверный вызов
`LimsLlmAssist.suggestAttributesFromNames` как есть — тот не завязан строго на
`fill_method`/`level` (`ExistingAttributeSummary` принимает их просто строкой,
для калибровки передаются пустыми — у `CalibrationAttribute` этих полей нет вовсе).
Чисто клиентская правка, sbe-llm/lab-service не менялись, деплоя бэкенда не было.

`npx tsc --noEmit`/`npm run build` — чисто. Живой вызов ИИ не прогонялся (нужен
реальный Obsidian-рантайм с ключом sbe-llm) — механизм эквивалентен уже проверенной
кнопке у обычных атрибутов, тот же серверный вызов. Версия 0.2.12 → **0.2.13**.

### 2026-08-26 — v0.2.12 (переименование сертификата поверки; привязка оборудование↔оборудование; параметры калибровки в конфигураторе метода)

Три доработки прямыми запросами пользователя, продолжение v0.2.11 в той же сессии.

**1. «Сертификат поверки» → «Сертификат аттестации»** — переименование заголовка блока (`renderEquipmentVerificationBlock`, kind='verification_cert'). «Акт поверки» не переименовывался — про него запроса не было.

**2. Привязка оборудование↔оборудование** (физическое прикрепление вспомогательного прибора к основному, напр. датчик к анализатору) — ОТДЕЛЬНО и независимо от `EquipmentMethodLink`/`method_equipment.role` из v0.2.11 (тот решает видимость блока калибровки ДЛЯ МЕТОДА; это — только группировку/отображение оборудования). Новая таблица `equipment_links` (`main_equipment_id, auxiliary_equipment_id`, many-to-many — один вспомогательный прибор может быть привязан к нескольким основным, CHECK против самопривязки). Новые эндпоинты: `GET /equipment-links` (вся таблица одним запросом — общий список строит из неё множество «скрыть с верхнего уровня» без N+1), `POST/DELETE /equipment/{id}/auxiliaries`. Оборудование, привязанное хотя бы к одному основному, больше не показывается отдельной карточкой верхнего уровня — только внутри карточки(-ек) своего(-их) основного(-ых) прибора(-ов) (полная вложенная карточка через `renderEquipmentCard(..., nested=true)`, один уровень вложенности — вложенная карточка не рендерит свой собственный список вспомогательных, во избежание рекурсии). На каждой карточке — оба направления привязки: «Прикрепить вспомогательный прибор» (эта карточка становится основной) и «Прикрепить к основному прибору» (эта карточка становится вспомогательной для выбранного) — один и тот же эндпоинт с разных сторон.

**3. «Параметры калибровки» в конфигураторе метода** — прямое продолжение решения из v0.2.11 («калибровка проводится сотрудниками лаборатории... так же как результаты испытаний, далее добавим блок настроек калибровки»). Новый раздел (9-я сворачиваемая секция) с двумя блоками: «Атрибуты калибровки» (`CalibrationAttribute` — id/название/тип, проще `MethodAttribute`: без fill_method/level/formula/aggregation — значение калибровки всегда вводится испытателем вручную один раз за запись, расчётов/агрегации нет) и «Форма для испытателя» (тот же паттерн, что у обычной формы результатов, но свой список атрибутов и свои универсальные системные поля — Дата калибровки/Температура воздуха в лаборатории/Атмосферное давление/Влажность воздуха, одни и те же для ЛЮБОГО метода, не заводятся как атрибуты). `methods.calibration_attributes`/`methods.calibration_operator_form` — новые JSONB-колонки, `MethodOperatorForm` переиспользован как есть.

Запись журнала калибровки расширена: `method_id` (чьи `calibration_attributes` применялись — оборудование может быть «Основное» сразу для нескольких методов, форма показывает select, если методов больше одного), `amb_temp`/`amb_pres`/`amb_moist` (универсальные поля, всегда), `values` JSONB (значения атрибутов выбранного метода). Журнал в карточке оборудования — новая колонка «Параметры» (сворачивает amb_*+values в одну строку по названиям атрибутов метода).

Подробности бэкенда (миграции, эндпоинты, живая E2E-проверка — привязка/отвязка/many-to-many, калибровка с атрибутами метода, откат тестовой конфигурации метода ГГ) — `lab-service/AGENTS.md`, та же дата.

`npx tsc --noEmit`/`npm run build` — чисто. `go build`/`vet`/`test` — чисто. Задеплоено на VDS, health ok. Версия 0.2.11 → **0.2.12**.

### 2026-08-26 — v0.2.11 (конфигуратор метода: сворачиваемые секции + графики-блоки; «Оборудование»: эксплуатация/поверка/калибровка/методы/документация; фикс безопасности — write-роль на смену статуса заявки)

Спека: `docs/superpowers/specs/2026-08-26-sbe-lims-configurator-equipment-design.md`.
Три прямых запроса пользователя одной сессией + фикс безопасности, добавленный в
план по ходу работы (найденный пробел из ревью `plugins/secrev.md`).

**1. Конфигуратор метода — все 8 блоков сворачиваемые, свёрнуты по умолчанию.**
Новый `renderCollapsibleSection(parent, title, onFirstOpen?)` (`lims-view.ts`) —
без `onFirstOpen` тело рендерится сразу (только визуально скрыто) — нужно
конфигуратору, чьи редакторы держат замыкания `redrawAttrs`/`redrawRules`/...,
обязанные существовать с первого рендера формы; с `onFirstOpen` — ленивый рендер
при первом раскрытии (тот же паттерн, что в sbe-apstore v0.3.6), используется
карточками оборудования (п.3) — не грузить калибровки/документы/методы для ВСЕХ
карточек сразу при открытии вкладки. Обёрнуты: Показатели метода, Импорт из
стандарта (ИИ), Загрузка из JSON, Атрибуты метода, Правила классификации,
Графики, Представление данных, Форма для испытателя.

**2. Графики метода — автоматически блоки представления, старая жёсткая панель
убрана.** Раньше «📈 Графики» в карточке заявки была отдельной панелью мимо
системы блоков документа (`presentation.blocks`) — без возможности скрыть/
показать по видам вывода или окружить текстом. Теперь при открытии
конфигуратора метода каждый `chart_config` без своего блока (по `chart_id`)
сразу получает блок в памяти (не на сервер, пока не нажата «Сохранить») —
виден в «Представление данных», доступны те же 3 галочки видимости и
редактирование, что у любого блока. Панель «📈 Графики» в `renderRequestDetail`
удалена целиком — графики показываются только через «Краткий вид»
(`renderShortView`, рендерит блоки с `show_in_ui`).

**3. «Оборудование» — карточки вместо таблицы, новые поля, калибровка,
привязка к методам, документация.** Свёрнутая карточка: код, название, дата
последней калибровки (если калибровок ещё не было — дата более свежей из
поверок). Раскрытая (переиспользует `renderCollapsibleSection` из п.1, ленивый
режим): расположение/ответственный/статус/дата ввода в эксплуатацию/
нормативный срок эксплуатации (редактируемые поля); сертификат и акт поверки
(номер+дата+скан, каждый со своей кнопкой загрузки); документация на
оборудование (список файлов, загрузка/удаление); привязанные методы (роль
Основное/Вспомогательное — select на месте, как у ролей сотрудников лабы, +
форма привязки нового метода); если карточка «Основное» хотя бы для ОДНОЙ
связи — дополнительно межкалибровочный период (своё значение на каждой
единице), плановая дата следующей калибровки (только чтение — считает
сервер), журнал калибровок (таблица + форма добавления записи: дата,
результат, необязательный файл). Бэкенд — `lab-service/AGENTS.md` (та же
запись 2026-08-26, там же — реальный баг PATCH-эндпоинта, найденный и
исправленный на живом E2E).

**4. Фикс безопасности (добавлен в план по ходу работы, по прямому запросу
пользователя): `POST /requests/{id}/status`.** Известный задокументированный
пробел — проверялась только видимость заявки, не право записи. Подробности,
E2E-проверка — `lab-service/AGENTS.md`. Клиент sbe-lims не менялся этим
пунктом (чисто бэковый фикс), но версия поднимается всё равно — пункты 1-3
этой записи клиентские, в той же сессии.

**5.** `npx tsc --noEmit`/`npm run build` — чисто. `go build`/`vet`/`test`
(lab-service) — чисто. Задеплоено на VDS, health ok. Версия 0.2.10 → **0.2.11**.

### 2026-08-26 — v0.2.10 (настройки: учётка почты email-приёма через ЦУП; фикс — target_group_compliance/combustibility_group не считались для метода ГГ)

**1. Учётка почты (`LAB_MAIL_*`) настраивается из плагина, не только правкой
`.env` на сервере.** Часть общей работы по закрытию ревью безопасности —
`LAB_MAIL_PASSWORD` был в плане `docs/superpowers/plans/2026-08-25-sbe-secrets-cup-plan.md`
как отложенный пункт («ротация вручную»). Новый раздел настроек «Приём
результатов по email (администратор)» (`settings-tab.ts`, виден только
admin/superadmin): включение приёма, IMAP-сервер, логин, пароль (никогда не
подтягивается обратно с сервера — write-only, пустое поле = «не менять»),
интервал опроса, карта методов (JSON). Значения уходят через
`getService('sbe-apstore').auth.setAppEnv('lab', {...})` — новый метод моста
(общий механизм ЦУП, `sbe-core/auth-service` `env_admin.go` + очередь
`app_env_pending`, применяет хост-скрипт `secret-applier.sh`: пишет `.env`,
пересоздаёт контейнер `lab`). Подробности механизма — `sbe-core/auth-service/AGENTS.md`
и `lab-service/AGENTS.md` (обе записи 2026-08-26). Плагин сам секретов не
хранит — ни в `data.json`, ни в вольте.

**2. Реальный баг, найденный на заявке 287/2026 (продолжение расследования
"почему не считается target_group_compliance").** Атрибут `target_group_compliance`
у метода ГГ не существовал вовсе (был только у ГВ) — заведён по аналогии. Но
попутно вскрылся код-баг: уже существующая формула `combustibility_group =
min_grade(...)` (5 classification-выходов метода ГГ) не считалась НИ ДЛЯ ОДНОЙ
заявки этого метода — `applyAggregatedFormulas` (`lab-service/results.go`)
считал формулы уровня `aggregated` ДО классификации, а не после, и формула,
зависящая от classification-выходов, детерминированно не находила свои
переменные (тихо пропускалась, лог только на сервере). Фикс —
`retryPendingAggregatedFormulas` + второй проход классификации после него
(нашлась трёхуровневая цепочка classification→formula→classification: сам
`target_group_compliance` тоже зависит от только что посчитанного
`combustibility_group`). Подробности, тесты, live-проверка на заявке 1378 —
`lab-service/AGENTS.md` (запись 2026-08-26). Клиент sbe-lims не менялся этим
пунктом — чисто бэковый фикс, но версия поднимается всё равно (пункт 1 этой
записи — клиентская правка в той же сессии).

**3.** `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.2.9 → **0.2.10**.

### 2026-08-25 — v0.2.9 (агрегация any/all для Да/Нет-полей; фикс — дубликаты вложенных файлов при повторной загрузке)

**1. Агрегация `any`/`all` для текстовых Да/Нет-полей.** Прямое продолжение фикса
v0.2.8 (заявка 287/2026, метод ГГ: `agg_burning_drops = max(burning_drops)` падал,
т.к. `burning_drops` — текстовое Да/Нет, не число) — пользователь предложил
конкретный удобный выбор: "если будет выбор 'Да если хоть в одной серии Да' и 'Нет,
если хоть в одной серии Нет' — это было бы удобно". Добавлены две новые DSL-агрегации
(`dsl.go`, `callExpr.eval`/`collectBools`, через уже существующий `toBool`, который
понимает "Да"/"Нет"/true/false/1/0): `any(field)` — "Да", если хоть в одной серии
"Да"; `all(field)` — "Да" только если ВО ВСЕХ сериях "Да" (т.е. "Нет", если хоть в
одной серии "Нет"). `validAggregationMethods`/`AttributeAggregation.Method`
(`lims_refs.go`) и клиентский тип + селект в конфигураторе метода (`lims-view.ts`,
подписи "Да\", если хоть одна серия \"Да\""/"Нет\", если хоть одна серия \"Нет\"")
синхронизированы в этом же коммите (тот же урок про формат+клиент одним коммитом,
см. запись v0.2.6 ниже). Саму формулу `agg_burning_drops` метода ГГ не трогали —
это правка конфига метода через UI, оставлена пользователю.

**2. Реальный баг, найденный по жалобе "в заявке 287/2026 сейчас 10 вложенных
файлов, хотя должно быть 2".** Расследование (фоновый агент + прямая проверка на
VDS) нашло: `files` для заявки 1378 содержала 10 строк — 2 РЕАЛЬНЫХ файла по 5 копий
каждый, идентичных по имени и размеру, залитых в 5 тесных серий (~0.2с внутри пары,
с интервалами от 5 до 50 минут между сериями) 2026-08-24. Причина — НЕ живой баг
почтового воркера (он выключен, `LAB_MAIL_ENABLED` не задан; буфер отложенных
результатов пуст), а следствие ручной отладки: одноразовый CLI `fetch-mail-photos`
(`mail_fetch_by_id.go`) перезапускался вручную ~5 раз по одному и тому же письму
(external_id=698) во время работы над предыдущим фото-фиксом. `saveResultSeries` уже
идемпотентен (`ON CONFLICT`), поэтому `measurement_results` не задвоилась — но
`uploadFileBytes` (`files.go`) НЕ имела вообще никакой проверки на уже загруженный
файл: каждый вызов грузил в S3 новый объект со случайным ключом и писал в `files`
новую строку, даже для байт-в-байт того же вложения. Фикс: `uploadFileBytes` теперь
проверяет `(request_id, file_name, file_size)` на совпадение с уже существующей
строкой `files` ДО загрузки в S3 и переиспользует найденную — новых загрузок/строк
для повторной обработки того же файла больше не создаётся. Затрагивает оба
вызывающих пути — общий HTTP-загрузчик файлов (`handleUploadFile`) и приём фото из
почты (`resolvePhotoAttachments`).
- Данные заявки 1378 почищены точечно (после явного подтверждения пользователем):
  удалены `files` id 1-8 (проверено — не используются ни в одной ссылке
  `measurement_results`), остались только id 9-10 (реально используемые в
  `photo_before`/`photo_after` серии 15). Заявка теперь показывает 2 файла.

**3. Тесты**: `dsl_test.go` (`TestDSLAnyAllAggregate` — any/all на смешанных/
однородных выборках), `lims_refs_test.go`
(`TestDeriveFormulasFromAttributesAnyAllAggregation` — сборка формулы `any(...)`/
`all(...)` из конфига атрибута). Дедуп `uploadFileBytes` юнит-тестом не покрыт — в
проекте нет инфраструктуры для мока БД/S3 (весь такой код здесь традиционно
проверяется по живым данным, не моками); проверено деплоем и прямой SQL-проверкой
на VDS. `go build`/`vet`/`test`, `npx tsc --noEmit`/`npm run build` — чисто.
Задеплоено на VDS (`docker compose up -d --build lab`), health `{"status":"ok"}`.
Версия плагина поднимается (затронуты и бэкенд, и клиент). Версия 0.2.8 → **0.2.9**.

### 2026-08-25 — v0.2.8 (график: целочисленные деления с минимальным шагом без наложения, легенда списком внутри поля графика; drag-reorder столбцов статической таблицы; фикс — одна сломанная формула больше не блокирует ВСЕ агрегированные расчёты и классификацию метода)

**1. Деления оси — только целые числа, минимально возможный шаг без наложения
подписей.** Прямой запрос пользователя: "деления всегда должны быть целые числа...
цена делений — минимально возможная, чтобы цифры не накладывались". Новый алгоритм
(`niceStepCandidates`/`resolveAxisTicks`, `charts.go`) перебирает шаг из округлого
ряда {1,2,5}×10ⁿ (1, 2, 5, 10, 20, 50, 100…) от самого мелкого к крупному и берёт
первый, при котором подписи делений не перекрываются в реально доступном месте
картинки (`fits`-колбэк — для оси X это ширина текста, для Y1/Y2 высота строки; для X
резолвится ПОСЛЕ вычисления margin'ов, т.к. только тогда известна доступная ширина
plotW). Побочный эффект: старая жалоба "-68.8 у температуры, где отрицательных
значений вообще нет" (v0.2.7) решена автоматически — целочисленный шаг от
неотрицательных данных не может дать отрицательное начало координат. Ручная
настройка шага в редакторе графика (v0.2.7) не тронута — если пользователь сам явно
вписал дробный шаг, он остаётся дробным (его осознанный выбор).

**2. Легенда — списком внутри поля графика, правый верхний угол.** Раньше легенда
была отдельной полосой НАД графиком (раздвигала картинку по вертикали, могла
занимать 2+ строки при переносе по ширине). По прямому запросу пользователя
("легенду нужно размещать в поле графика списком... предпочтительное расположение —
правый верхний угол") — `drawLegendBox` (`charts.go`) рисует список ПОСЛЕ серий,
одной колонкой, с непрозрачным белым фоном и рамкой внутри области построения, у
правого верхнего края — подписи остаются читаемыми, даже если легенда физически
перекрывает линию данных под ней.

**3. Drag-reorder столбцов у статической таблицы.** У динамической таблицы
(`RichNode "table"`, данные серий) перетаскивание колонок уже было (v0.2.3). У
статической (`"static_table"`, ручной ввод) — не было вовсе, только кнопки ✖/➕. По
прямому запросу пользователя добавлен тот же паттерн (⠿-хэндл, dragstart/dragover/
drop) в `renderStaticTableEditor` (`block-editor.ts`) — переставляется i-й элемент
КАЖДОЙ строки `node.rows` (строка → колонка → содержимое ячейки), не единый массив.

**4. Реальный баг, найденный по жалобе "не применяются расчётные и
классификационные правила к заявке 287/2026" (id=1378, метод ГГ).** Расследование
(фоновый агент + прямая проверка на VDS) нашло: метод ГГ настроен с формулой
`agg_burning_drops = max(burning_drops)`, где `burning_drops` — текстовое поле
Да/Нет, а НЕ число (ошибка конфигурации метода — числовая агрегация `max` над
нечисловым полем). `applyAggregatedFormulas` (`results.go`) считал формулы уровня
"aggregated" в цикле с `return err` на первой же ошибке — из-за этого при падении
ОДНОЙ формулы ("не число «Нет»") ни одна из ОСТАЛЬНЫХ формул (в списке ПОСЛЕ
сломанной), ни `applyAggregatedClassification` (вызывается только после успешного
завершения всего цикла) не выполнялись НИКОГДА — ни для этой заявки, ни для ЛЮБОЙ
другой заявки метода ГГ, с момента добавления этой формулы в конфиг метода. Ошибка
при этом тихо уходила в лог (`log.Printf`), а HTTP-ответ отдавал `{"ok": true}` —
для пользователя расчёт выглядел успешным, но `aggregated_results` оставался
устаревшим (значения до добавления сломанной формулы), классификация — пустой.
Фикс: цикл вынесен в чистую (без обращения к БД, юнит-тестируемую) функцию
`evalAggregatedFormulas` — падение одной формулы теперь просто пропускает ЕЁ цель
(с логом) и продолжает считать остальные; классификация выполняется с тем, что
успело посчитаться. `agg_burning_drops` сам по себе остаётся неопределённым (пока
не число, посчитать нечем) — это отдельный вопрос конфигурации метода (нужна
осмысленная агрегация для Да/Нет-поля, не `max`/`min`/`avg`), не код-баг; не
трогали без уточнения у пользователя, какая семантика ожидается (напр. "Да, если
хоть одна серия = Да").

**5. Тесты**: `results_test.go` —
`TestEvalAggregatedFormulasSkipsFailingFormulaContinuesRest` (формула ДО и формула
ПОСЛЕ сломанной обе досчитываются; сломанная — отсутствует в результате), точно
воспроизводит реальный сценарий заявки 287/2026 и реальный текст ошибки. `go build`/
`vet`/`test`, `npx tsc --noEmit`/`npm run build` — чисто. Задеплоено на VDS
(`docker compose up -d --build lab`), health `{"status":"ok"}`. Версия плагина
поднимается (затронуты и бэкенд, и клиент). Версия 0.2.7 → **0.2.8**.

### 2026-08-25 — v0.2.7 (график: убран паразитный блок без данных; подписи осей слева/справа + числа делений + поворот на 90°; ручная настройка начала отсчёта и шага деления по каждой оси)

**1. Паразитный блок "📈 Графики" со сломанной картинкой.** Найдено по жалобе
пользователя ("удалил блок Графики, а он всё равно рендерится в UI"): `chart_configs`
принадлежат МЕТОДУ, а не отдельной заявке — раздел "Графики" в деталях заявки
(`lims-view.ts`) раньше перебирал ВСЕ `chart_configs` метода безусловно для КАЖДОЙ
заявки этого метода, даже когда у конкретной заявки ещё не набрались данные для
конкретного графика (сервер отвечал 404 "нет данных для графика") — заголовок и
сломанный `<img>` показывались у каждой такой заявки. Теперь заголовок и картинки
рендерятся, но если ни один график в итоге не загрузился (все `<img onerror>`) — весь
блок целиком убирается через `remove()`.

**2. Подписи осей и шкала деления — три отдельных фикса по одной жалобе
пользователя.** Раньше `yLabel` рисовалась поверх графика горизонтально, деления оси
имели только линии сетки без чисел значений:
- Подпись оси Y — слева от делений основной оси, подпись оси Y2 — справа от делений
  второй оси (макет `[подпись Y] [деления Y] [график] [деления Y2] [подпись Y2]`),
  а не произвольно поверх картинки.
- У делений обеих осей (и X) появились подписанные числовые значения
  (`formatTickValue`), раньше рисовалась только линия сетки.
- Подпись оси Y/Y2 повёрнута на 90° против часовой стрелки (общепринятая ориентация,
  читается снизу вверх) — собственный рендерер не умеет поворачивать глифы напрямую,
  поэтому текст сначала рисуется горизонтально на отдельном прозрачном холсте нужного
  размера (`drawVerticalText`), затем холст целиком поворачивается (`rotateImage90CCW`,
  побайтовый remap пикселей) и накладывается на итоговое изображение.

**3. Ручная настройка шкалы деления по каждой оси.** Пользователь заметил, что
авто-диапазон с 10%-м отступом от данных уводил шкалу температуры в отрицательные
значения (напр. -68.8), хотя реальных отрицательных значений в данных вообще не было
— затем уточнил запрос: нужна не только защита от лишнего отступа, а полноценная
ручная настройка **точки начала отсчёта и цены деления** для каждой оси отдельно.
`chartAxisSpec{Min, Step}` + `chartAxisOverrides{X, Y1, Y2}` (`charts.go`,
`chartAxisSpecFromConfig` читает `{x,y,y2}_axis_{min,step}` из `chart_config`) —
`resolveAxisTicks`: без `Step` — прежние 6 равных делений с 10%-м отступом, но больше
не пересекающие ноль в сторону, где данных нет (сам фикс исходной жалобы); c `Step` —
число делений становится переменным, деления идут от `Min` (или от `dataMin`,
округлённого вниз до кратного шагу, если `Min` не задан) с этим шагом до покрытия
`dataMax`.

**4. Фронтенд синхронизирован в ТОЙ ЖЕ сессии** (см. урок из записи v0.2.6 ниже про
два прежних инцидента потери полей конфига при пересохранении) — `ChartConfig`
(`types/lims.ts`) получил `x_axis_min`/`x_axis_step`/`y_axis_min`/`y_axis_step`/
`y2_axis_min`/`y2_axis_step`; редактор графика (`lims-view.ts`) — по жалобе
пользователя "непонятно какие подписи к чему относятся" — раньше подписи осей были
безымянными полями, различимыми только по placeholder'у, а подпись Y2 вообще пряталась
в отдельном месте внутри режима "по времени". Теперь единый явный список "Оси:" с
тремя подписанными строками (`renderAxisSettingsRow`) — "Название оси X"/"Название оси
Y1"/"Название оси Y2", у каждой — название + начало отсчёта + шаг деления, видны
независимо от режима графика.

**5. Тесты**: `charts_test.go` — `resolveAxisTicks` (авто-диапазон не пересекает ноль в
сторону, где данных нет, ни для положительных, ни для отрицательных данных; `Min` без
`Step` сдвигает начало 6-частной шкалы; `Min`+`Step` дают переменное число делений от
заданного начала). `go build`/`vet`/`test`, `npx tsc --noEmit`/`npm run build` — чисто.
Задеплоено на VDS (`docker compose up -d --build lab`), health `{"status":"ok"}`.
Версия плагина поднимается (затронуты и бэкенд, и клиент). Версия 0.2.6 → **0.2.7**.

### 2026-08-24 — v0.2.6 (данные прибора из письма; графики: текст/легенда/вторая ось; новый тип атрибута "Временной ряд"; аудит Go-лицензий)

**1. Показания прибора (statistics/experiment_params) — раньше терялись целиком.**
Письмо прибора (Comb, содержит `mesure_data`) до этого фикса пропускалось ПОЛНОСТЬЮ
("не MVP", решение 2026-08-19) — но, как заметил пользователь на живом письме
(external_id=698), кроме сырых массивов по каналам оно несёт `statistics.by_channel`
(максимальная температура/время пика по каждому каналу) и `experiment_params`
(длительность этапов, число каналов) — это НЕ сырой сигнал, а реальные показатели
метода ГГ, для которых атрибуты `tp1-4_smog`/`temp_of_smog`/`time_of_max_temp` уже
существовали в схеме, но всегда оставались пустыми. `extractInstrumentFields`
(`email_ingest.go`) достаёт эти под-поля; `time_of_max_temp` — среднее по каналам (по
решению пользователя, тот же принцип, что уже даёт `average_max_temp`). Добавлены 5
новых атрибутов метода ГГ под `experiment_params` (pre/main/post_time_seconds,
num_channels, poll_interval_seconds).
- **Письмо-форма и письмо прибора одного измерения делят один `series_num`** — раньше
  игнорировался (всегда auto-increment), из-за чего создавались ДВЕ строки вместо
  слияния в одну. Теперь `applyResultPayload` явно читает `series_num` письма и грузит
  уже сохранённые values той же серии (`loadSeriesValuesAt`, `results.go`) перед
  слиянием новых полей.
- **Реальный баг, найденный при проверке**: письмо-форма несёт `"tp1_smog":""` как
  заглушку "заполнит прибор" — при порядке обработки "прибор, потом форма" (порядок
  писем одной серии НЕ гарантирован) пустая строка формы затирала уже сохранённое
  число от прибора. Исправлено `setIfMeaningful` — пустое значение никогда не затирает
  уже сохранённое непустое (тот же принцип восстановления ЕКН этой сессии — "первое
  непустое значение выигрывает").

**2. Новый тип атрибута `data_type="timeseries"`** — значение не число/текст, а весь
ряд датчика целиком (`{time, channels, average_temp, derivative}`), заполняется
автоматически (synonyms на `mesure_data`, тот же механизм, что у фото). Показывается
содержательно ТОЛЬКО через график — исключён из обычных таблиц и плейсхолдеров
протокола (там раньше выводился как дамп JSON одной строкой — жалоба пользователя "все
данные внесены в одну ячейку").

**3. График — реальный текст (заголовок/подписи осей/легенда) и вторая ось Y.**
Обнаружено по жалобе "невозможно изменить название графика... не отображается
легенда": `renderChart` (`charts.go`) принимал `title`/`xLabel`/`yLabel` и тут же их
отбрасывал (`_ = title` и т.п.) — редактор сохранял значения корректно, рендер их
никогда не рисовал. Добавлен `golang.org/x/image/font/opentype` + встроенный шрифт Go
Regular (поддерживает кириллицу) — заголовок, подписи осей, легенда с цветными
квадратиками и переносом по ширине.
- **Вторая (правая) ось Y** (`chartSeries.Y2`, `y2_label`) — по прямому запросу
  пользователя ("наложение графика производной на график температуры"): ряды разного
  порядка величины (температура ~20-900°C, производная ~±40) теперь можно показать на
  одном изображении, каждый со своим масштабом, без взаимного искажения.
- **Конфиг графика переработан на `timeseries_series`** — список НЕЗАВИСИМЫХ рядов
  (каждый со своим `source_param`+`channel`+`axis`), не общий `source_param`+`channels`
  на весь график — по прямому запросу пользователя учесть будущий случай "два и более
  графика... ось X в одних единицах, но пары X-Y не совпадают" (напр. второй датчик с
  другой частотой опроса) — каждый ряд тянет свой собственный `time`-массив из своего
  атрибута, ничего не предполагает об остальных рядах.
- Метод ГГ: температура (4 канала + среднее) и производная (вторая ось) — теперь ОДИН
  график `smoke_temp_chart`, не два раздельных.

**4. Два реальных инцидента с потерей конфига графика при пересохранении в
интерфейсе — важный урок.** Оба раза одна и та же причина: формат `chart_config`
меняется на сервере раньше, чем в клиентском коде — любое открытие+сохранение
конфигуратора между этими шагами стирает поля, которых типы/редактор ещё не знают
(`cfg.chart_configs.map(...)` — пересобирает объект только по известным клиенту
полям). Первый раз — с `kind`/`channels` (пользователь сам обнаружил и сообщил, "я
сломал графики, сам не верну"); второй раз — при переходе на `timeseries_series` (тот
же механизм, найден по жалобе "блок Графики отображается без графиков" — конфиг в базе
откатился к старой форме). Оба раза восстановлено вручную по SQL, редактор досогласован
с сервером. **Урок на будущее**: менять формат `chart_config` на сервере и в
клиентском типе/редакторе строго ОДНИМ коммитом/сессией, не оставлять промежуточное
состояние, даже кратко.

**5. Аудит Go-зависимостей на свободные лицензии** (по прямому запросу пользователя,
после добавления `golang.org/x/image`) — проверены все прямые и транзитивные
зависимости `lab-service` (GitHub API `license.spdx_id`, не по памяти): MIT
(`go-imap`, `golang-jwt/jwt`, `pgx`, `go-sasl`, `pgpassfile`/`pgservicefile`/`puddle`,
`mohae/deepcopy`), BSD-3-Clause (`excelize`, `xuri/efp`/`nfp`, все `golang.org/x/*`),
Apache-2.0 (`richardlehane/mscfb`/`msoleps`) — ни одной с ограничениями. Правило внесено
в корневой `plugins/AGENTS.md` ("Только свободные Go-библиотеки").
- Побочный фикс: `go mod tidy` локально поднял `go.mod` до `go 1.25.0` (транзитивно,
  вслед за апгрейдом `x/text`/`x/sync` при добавлении `x/image`), хотя реально ничего
  из кода этого не требует — деплой упал (образ использует `golang:1.24-alpine`,
  `GOTOOLCHAIN=local`). Пофиксено: `x/text`/`x/sync` возвращены к прежним версиям,
  директива `go.mod` вручную опущена до `go 1.24` (`go build` после этого проходит
  чисто, `go mod tidy` больше НЕ запускать — сам поднимает её обратно).

**6. Тесты**: `email_ingest_test.go` (`extractInstrumentFields`, `setIfMeaningful` —
регресс на реальном "tp1_smog":"" сценарии), `charts_test.go` (`timeseries_series` с
независимыми X-рядами, вторая ось, `renderChart` с заголовком/подписями не падает).
`go build`/`vet`/`test` — чисто; `npx tsc --noEmit`/`npm run build` — чисто. Проверено
на живых данных заявки 1378: график с 6 рядами, заголовком, легендой (перенесённой на 2
строки), двумя осями — рендерится корректно (PNG идентичен при повторной генерации).
Версия плагина ПОДНИМАЕТСЯ (затронут и бэкенд, и клиент — не чисто бэковое изменение).
Версия 0.2.5 → **0.2.6**.

### 2026-08-24 — фото из письма реально загружаются и отображаются (HTML + DOCX); чисто бэк, версия не поднимается

Продолжение записи v0.2.5 ниже — тогда фото были заведены как атрибут (synonyms →
values), но реально не отображались: значение — яндекс-ссылка письма
(`https://forms.yandex.ru/cloud/files?path=...`), а прямой HTTP-запрос к ней без
авторизации отдаёт `302` на `auth.cloud.yandex.ru/oauth/authorize`, дальше — капча
Яндекса. Пользователь подтвердил (открыв письмо напрямую): реальное фото приложено к
письму ФАЙЛОМ, а не только ссылкой — расследование подтвердило: ни один инструмент в
проекте (включая сам Go-бэкенд) до этого фикса не смотрел дальше text/plain и text/html
частей MIME, вложения читались и тихо отбрасывались (`extractMultipart`, `switch mt`
без `default`).

**1. Извлечение вложений (`email_ingest.go`).** `extractMailText`/`extractMultipart`
разделены на общее ядро (`extractMailCore`/`extractMultipartCore`, теперь возвращает и
`[]mailAttachment`) + тонкие обёртки: `extractMailText` (сигнатура не меняется,
используется `peekPayload`, где вложения не нужны) и новая
`extractMailTextAndAttachments` (используется `processMessage`). Новый `default`-case в
`switch mt` — вложение с непустым `part.FileName()` сохраняется (без имени — не
угадываем, не сохраняем). `decodeTransferEncoding` вынесен из `decodePart` отдельно —
бинарные байты вложения нельзя пускать через charset-декодер (это делают только для
текстовых частей).

**2. Сопоставление вложения с photo_before/photo_after — по хвосту имени файла, НЕ по
точному совпадению.** Живой прогон на реальном письме (external_id=698, folder=Comb)
показал: имя вложения — `17875666329476692133998677536992.jpg`, а имя файла в
яндекс-ссылке — `6a8c1a2f902902b1f7c9d9a4_17875666329476692133998677536992.jpg`: Яндекс.
Формы добавляет свой хеш-префикс при заливке в облако, вложение письма хранит исходное
имя без префикса. `matchAttachmentByFilename` — `strings.HasSuffix`, не `==`; 0 или
больше одного совпадения — не гадаем (та же дисциплина, что `resolveResultKey`).
`extractYandexFormsFilename` разбирает `path=` из ссылки. `matchPhotoFields`/
`resolvePhotoAttachments` — сопоставленные вложения грузятся в объектное хранилище
(`uploadFileBytes`, см. п.3) и подставляются В `values` (через тот же synonyms-путь, что
уже был) ВМЕСТО мёртвой яндекс-ссылки; не сопоставилось/пусто — поле не пишется вовсе
(не пишем то, что заведомо не откроется). Тот же путь пробросен и в буфер отложенных
результатов (`email_ingest_pending_results` — новая колонка `attachments JSONB`,
`enqueuePendingResult`/`retryPendingResults`), иначе письмо, буферизованное до появления
заявки, потеряло бы фото к моменту повторной попытки.

**2а. Побочная находка (та же папка Comb): у одной заявки — несколько писем с одним
external_id (форма испытателя + отдельно сырая телеметрия прибора).** Обычный воркер это
уже различал (`mesure_data` → `skipped_signal`, решение 2026-08-19), но новый одноразовый
инструмент восстановления (см. п.4) сперва наивно требовал ровно одного совпадения по ID
— пришлось научить его тому же фильтру (`type="result"`, без `mesure_data`).

**3. S3-бакет НЕ публичный — прямая ссылка `s.s3.Put()` тоже отдаёт 403.** Изначальное
предположение ("документы уже так делают") оказалось неверным: `documents-service`
использует тот же паттерн S3+`file_url`, но потребляет его через собственный
авторизованный вызов из кода плагина, а не через пассивный `<img src>` в чужой HTML —
поэтому там 403 никогда не всплывал. `rclone link` (presigned-ссылка) — работает на
`GET`, но НЕ на `HEAD` у этого Ceph-гейтвея (первая проверка `curl -I` дала ложный 403,
`curl` без `-I` — реальные байты фото с верным `Content-Type: image/jpeg`); максимум
срока — 1 неделя (жёстко ограничено этим rclone/Ceph). Постоянно хранить presigned-URL в
`values` было бы просто более медленным повторением исходной проблемы (ссылка снова
протухла бы). Решение: `S3Store.Link` (новый метод, `rclone link --expire`) + новый
`GET /api/lab/file-redirect?key=...` — БЕЗ `requirePerm` (сознательно: `<img src>` в
`srcdoc` sandboxed iframe не может приложить `Authorization`; защита — случайность ключа
объекта, тот же уровень, что у "неопубликованной" облачной ссылки, но реально
работающий) — выпускает свежую presigned-ссылку на КАЖДЫЙ заход и 302-редиректит; сама
ссылка нигде не хранится, значит не может протухнуть на хранении. `uploadFileBytes`
(`files.go`, был добавлен в прошлой записи) теперь возвращает именно эту стабильную
ссылку, а не прямой адрес объекта — правится и для общего (не только фото) пути загрузки
файлов, т.к. у него сегодня всё равно нет потребителей на клиенте (dead code), менять
безопасно. По решению пользователя загруженные фото также регистрируются в общей таблице
`files` заявки (`uploadedBy` — адрес почтового ящика для email-ingestion).

**4. Восстановление фото уже принятой заявки 1378 (external_id=698) — без повторного
`fetch-mail`** (он не идемпотентен, создал бы дублирующую серию, см. запись v0.2.5).
Новый `./lab-service fetch-mail-photos -folder=Comb -id=698 -request=1378`
(`mail_fetch_by_id.go`, переиспользует `searchMatchingSeqNums`, общий с `fetch-mail`) —
только извлекает/грузит фото и печатает URL, НЕ вызывает `processMessage`/
`saveResultSeries`. Применено вручную проверенным `UPDATE measurement_results` с
результатом (обе ссылки перепроверены `curl` — `200 image/jpeg` — до применения).

**5. DOCX-экспорт — реальные встроенные картинки, не текстовая пометка** (по прямому
запросу пользователя: "при экспорте в doc тоже нужно реализовать экспорт изображений (не
ссылок)"; раньше — задокументированное, сознательное ограничение, см. комментарий у
`renderBlockChartDocx`). У ручного OOXML-генератора (без библиотеки, `archive/zip` +
руками собранный XML) не было НИ media-частей, НИ непустого `word/_rels/document.xml.rels`
— добавлены с нуля: `docxPhotoRegistry` (общий на ВЕСЬ протокол, не на метод — иначе
relationship id/имена `word/media/imageN.*` пересеклись бы между методами документа),
`docxDrawingXML` (namespace wp/a/pic/r объявлены прямо на использующих их элементах, не
трогая корневой `<w:document>`), `docxImageExtent` (пропорции по `image.DecodeConfig` —
квадратная рамка 2×2 дюйма, реальный прямоугольник вписан с сохранением соотношения
сторон, не растянут). `protocolDocx` стал методом `*Server` (нужен `s.s3`) —
`renderTableDocx`/`renderInlineDocxRuns` получили тот же `data_type="photo"`-путь, что
уже был у HTML (`renderTableHTML`/`renderInlineHTML`). Проверено на живых данных заявки
1378: `.docx` — валидный zip, 2 части `word/media/*.jpg` (байты совпадают с
загруженными), 2 relationship, 2 `r:embed` в `document.xml`, XML well-formed, пропорции
сохранены (`cx=1371600 cy=1828799` — портретное фото, не квадрат).

**6. Тесты** (email_ingest_test.go, protocol_test.go — все чистые функции, без
БД/S3/IMAP): извлечение вложения из multipart (включая вложенный multipart и
неопознанную безымянную часть), `extractYandexFormsFilename`, `matchAttachmentByFilename`
(включая регресс на реальном хеш-префиксе Яндекса), `matchPhotoFields`,
`fileRedirectKey`, `docxImageExtent` (сохранение пропорций + safe fallback на
нераспознанных байтах). `go build`/`vet`/`test` — чисто, `go test` живьём на VDS не
запускался отдельно (одна и та же сборка), задеплоено (`docker compose up -d --build
lab`), проверено HTTP-запросами к живому серверу (не только юнит-тестами) — HTML и DOCX
протокол реально отдают рабочие фото.

Версия плагина НЕ поднимается — чисто бэковые изменения в `lab-service/` (правило
корневого `plugins/AGENTS.md`, добавлено в прошлой записи), сам плагин (клиент,
`main.js`) не менялся.

### 2026-08-24 — v0.2.5 (восстановление ЕКН 129 заявок; тестовый приём почты ГГ/РП; фото в протоколе; фикс статистики серий)

**1. Восстановление реальных ЕКН/названий материала для 129 заявок.** Найдено
пользователем на живом кейсе ("почему во всех заявках произошла подмена ЕКН") —
причина в объединении "безымянных" объектов 2026-08-21 (см. запись ниже, "469 →
134 объекта"): исторический импорт LPITrack всегда оставлял `objects.name`
пустым независимо от реального товара, поэтому ВСЕ такие объекты (129 заявок)
слились в один канонический (`id=955`, `ekn=644712`) — верно только для 5 из
129, у остальных 124 — другой реальный товар. Восстановлено по исходным
письмам LPITrack (`mail_records.jsonl`, кросс-референс по `external_id`):
`pg_dump`-бэкап → 46 групп по реальному ЕКН → 5 заявок остались на 955
(переименован), 9 переведены на 2 уже существующих объекта, 115 — на 42 новых
объекта; `requests.title` каждой заявки — её собственное описание товара, не
общее для группы. Подтверждено точечно (заявки 671/698 из следующего пункта
теперь показывают разные, верные ЕКН).

**2. Тестовый приём почты (методы ГГ/РП) — по прямому запросу пользователя,
без сбора всей почты.** Новый пароль почты (`lpitn@yandex.ru`, отдельный
app-пароль Яндекса — НЕ пароль десктопной версии LIMS_LPI) — только в
`/opt/mailers/.env` на сервере, `LAB_MAIL_ENABLED=false` (постоянный воркер
не включён — пользователь выбрал точечный режим вместо постоянного опроса).
Атрибуты методов ГГ (id=1, 23 атрибута) и РП (id=3, 5 атрибутов) перенесены из
реальных писем папок Comb/FlamProp (`update_methods_gg_rp.sql`); у ГГ
`damage_length_1/2` заменены на реальные `comb_length_1..4`, `mass_after`
исправлен text→int; у РП калибровочные массивы (`calibration_length`/
`calibration_flux_*`) сознательно НЕ заведены — прямое решение пользователя
("этот вопрос в разрабатываемой системе будет реализован иначе... вводим
только прямые измерения и расчеты").
- Новый `lab-service/mail_fetch_by_id.go` — точечный CLI-режим
  `./lab-service fetch-mail -folder=Comb -id=698` (по выбору пользователя —
  вместо постоянного воркера). `loadEmailIngestConfig` разделён на обёртку
  (проверяет `LAB_MAIL_ENABLED`) и `loadEmailIngestConfigCore` (без проверки,
  нужна точечному режиму). **Найден и исправлен реальный баг**: на папке в
  500+ писем скачивание тел ВСЕХ писем одним `Fetch` обрывает соединение
  Яндекса (`imap: connection closed`, падало на 175/524) — теперь сначала
  серверный IMAP `SEARCH BODY <id>` (кандидаты, без скачивания тел), и только
  затем `Fetch` тела кандидатов + строгая проверка встроенного JSON `"ID"`.
  Успешно получены оба целевых письма — заявка 698 (2 письма Comb: форма
  испытателя применена, сырая телеметрия прибора корректно тихо пропущена по
  уже существующему решению 2026-08-19) и заявка 671 (3 письма FlamProp).
- `docker-compose.yml` (sbe-core, без своего git) — добавлена пробрасываемая
  переменная `LAB_MAIL_*` в сервис `lab` (её не было вообще — переменные в
  `.env` сами по себе ничего бы не давали).

**3. Баг статистики/нумерации серий — найден на реальных данных (3+ письма
подряд по заявке 671).** `nextSeriesNum` считал следующий номер серии по MAX
`series_num` СО стат-строкой included → настоящая серия занимала номер "после"
стат-строки; `recomputeStatistics` пересчитывал номер своей строки как
`len(series)+1`, что при разрывах совпадало с номером только что вставленной
настоящей серии → `duplicate key value violates unique constraint
uq_meas_req_method_series`. Реальные измерения не терялись (upsert по
`series_num` шёл в новую свободную ячейку), но стат-строка пропадала. Оба места
(`results.go`) теперь считают следующий номер по MAX **среди `is_statistical_row
= false`** — согласованно, коллизий больше нет. Точечно исправлены уже
испорченные данные заявки 671 (нумерация 1,3,4→1,2,3, стат-строка
восстановлена, калибровочные ключи убраны из `values` — попали туда до фикса
п.4) — с подтверждением пользователя перед правкой (прямое изменение
production-данных).

**4. Отброс незаведённых полей письма + фото в протоколе.** Email-приём
раньше клал В values ЛЮБОЕ поле письма, не входящее в служебный список
(`resultMetaFields`/`systemRequestFields`), включая калибровочные массивы у
РП — прямое нарушение решения п.2. Новый `loadAttributeIDSet` (`lims_refs.go`)
+ фильтр в `applyResultPayload`: поле сохраняется, только если резолвится в
атрибут, реально объявленный в `methods.input_parameters` этого метода.
`photo_before`/`photo_after` (URL фото из письма-формы, Yandex Forms) убраны
из `resultMetaFields` — теперь идут через тот же synonyms-механизm, что и
любое другое поле (у ГГ настроен синоним на `photo_before_test`/
`photo_after_test`, `add_photo_synonyms_gg.sql`), и благодаря этому попадают в
values и рендерятся в протоколе как `<img>` (уже умел `renderTableHTML` —
не хватало пути "письмо → values"). `renderInlineHTML` (`protocol.go`) —
добавлен тот же `<img>`-рендер для плейсхолдера фото ВНЕ таблицы (раньше URL
просто печатался как escaped-текст). Точечно довставлены фото в уже принятую
серию заявки 698 (URL взят из её собственных колонок `photo_before/after`, без
повторного скачивания письма — CLI-режим не идемпотентен, повтор создал бы
дублирующую серию).

**5. UX конфигуратора отчёта — фото было "не понятно, как вставлять".**
Причина: photo-атрибут в общем списке ничем не отличался от текстового, а при
вставке ВНЕ таблицы предлагался полный выбор "среднее/минимальное/
максимальное/первая/последняя серия" — для изображения три первых пункта не
имеют смысла. `block-editor.ts`: новый `attrDisplayName()` — префикс "📷" у
`data_type: "photo"` во всех списках/пикерах (плейсхолдер, колонки таблицы,
уже вставленный чип); для фото при вставке вне таблицы — только "первая/
последняя серия" (`renderAggChoice(attr, photoOnly)`).
- `go build`/`vet`/`test` (lab-service) — чисто; `npx tsc --noEmit`/
  `npm run build` (sbe-lims) — чисто. Версия 0.2.4 → **0.2.5**. Задеплоено на
  VDS (`docker compose up -d --build lab`).
- Известный нерешённый пробел: DOCX-экспорт протокола фото не встраивает
  (нужна отдельная задача — скачивание байтов изображения и embed в OOXML
  media/relationships; HTML-рендер фото показывает).

### 2026-08-24 — v0.2.4 (lab_admin — реальный делегированный админ своей лабы; редактирование ролей на месте)

Найдено пользователем на живом кейсе: у shoya.vs@tn.ru роль `lab_admin` (в
`lab_members` своей лабы), но конфигуратор методов/«Сотрудники»/роль
руководителя в канбане были ему недоступны. Причина — две независимые системы
ролей в lab-service: глобальная (`lab_permissions`: viewer/editor/admin/
superadmin) и лабораторная (`lab_members`: lab_operator/lab_admin/lab_auditor).
Все admin-гейты (метод-конфиг, «Сотрудники», Kanban-руководитель) проверяли
только ГЛОБАЛЬНУЮ роль — `lab_admin` был функционально синонимом
`lab_operator`, несмотря на название (задокументированный, но нереализованный
пробел «делегированные полномочия lab_admin», см. записи 2026-08-19/23-24 ниже
и в специфике «Роли»).

**1. lab_admin теперь полноценный админ СВОЕЙ лабы** (по решению пользователя:
любая роль сотрудникам своей лабы, включая назначение другого lab_admin;
создание+конфигурирование+удаление методов своей лабы — без прав над чужими
лабами). Сервер (`lab-service`): новые `requireLabAdminOf`/`requireLabAdminOfAny`/
`requireLabAdminOfAll` (`lims_refs.go`) — app-admin+ ИЛИ lab_admin
соответствующей лабы(лаб); применены в `handleCreateMethod`
(`requireLabAdminOfAll` по запрошенным `lab_ids` — не привязать метод к чужой
лабе), `handleUpdateMethodConfig`/`handleDeleteMethod` (`requireLabAdminOfAny`
по ТЕКУЩИМ лабам метода — достаточно администрировать одну из нескольких),
`handleSetLabMember`/`handleRemoveLabMember` (`requireLabAdminOf` по `lab_id`
операции). Route-гейты этих 5 эндпоинтов ослаблены с `requirePerm("admin")` до
`requirePerm("editor")` — реальная проверка теперь внутри хендлера, не на
уровне глобальной роли (тот же паттерн, что уже был у `kanban-move`/
`lab-members?lab_id=`). `canApplyKanbanMove` (`kanban.go`) — lab_admin ИМЕННО
этой лабы теперь тоже проходит в "свободную" ветку руководителя (было: только
глобальный admin/superadmin, lab_admin — синоним lab_operator). Новый
`methodLabIDs(methodID)` (`references.go`) — лабы одного метода, без карты по
всем методам. Новый тест `TestCanApplyKanbanMoveLabAdminActsAsLabHead` — 10/10
зелёных (сборка+тесты — до и после деплоя, VDS пересобран, health ok).
- Клиент (`lims-view.ts`): «Методы» — новый `myAdminLabIds()` (лабы, где
  пользователь lab_admin) + `isAdminOf(labIds)` на каждую карточку метода
  (кнопки «✎ Конфиг»/«✖ Удалить» видны, если админ хотя бы одной лабы метода);
  форма создания метода — чекбоксы лабораторий сужены до администрируемых
  (не даёт даже попытаться выбрать чужую, хотя сервер и так отклонит).
  «Сотрудники» и Kanban-доска — `isLabHead`/гейт страницы теперь
  `this.canAdmin || myRoleIn(roster) === 'lab_admin'` (было — только
  `canAdmin`). `renderLabCheckboxes` получил опциональный параметр списка лаб
  (по умолчанию — все, для lab_admin — только свои).

**2. Редактирование роли сотрудника на месте** (по прямому запросу
пользователя: "фактически это можно сделать только через удаление/
добавление" — сервер (`handleSetLabMember`) уже был upsert-ом, чего-то не
хватало только в UI). «Сотрудники» (`renderLabMembers`) и нативная вкладка
настроек Obsidian (`settings-tab.ts`, «Права доступа») — статичный текст роли
заменён на `<select>` (тот же паттерн, что уже был у глобальных ролей в
sbe-requests), меняет роль сразу по `change`, без удаления записи. Заодно
добавлен пропущенный вариант `lab_auditor` в формы добавления обоих экранов
(был только `lab_operator`/`lab_admin`). Новый общий список опций
`LAB_MEMBER_ROLE_LABELS` (модульная константа, `lims-view.ts`) — используется
и формой добавления, и inline-редактированием, чтобы не дублировать список.
- `renderLabMembers`/`renderLabMemberForm`: `listLabMembers()` без аргумента
  (только для admin) заменён на `fetchLabRoster(this.labId)` — иначе lab_admin
  (без глобальной роли admin) получил бы 400 от сервера (`lab_id is required`)
  при попытке открыть «Сотрудники».
- `npx tsc --noEmit`/`npm run build` — чисто; `go build`/`vet`/`test` в
  lab-service — чисто, 10 тестов зелёных. Версия 0.2.3 → **0.2.4**.

### 2026-08-24 — v0.2.3 (Kanban-доска «Очередь лаборатории»; 5 доработок блочного редактора: таблицы/индексы/выравнивание/drag-list/полный номер)

Два независимых блока работы, оба по прямым запросам пользователя.

**1. Kanban-доска «Очередь лаборатории».** Раньше это была плоская страница
(`renderRequests`, фильтр `new`/`received`) — статус `processing` не показывался
вообще ни на одной специализированной странице. Теперь `renderQueueBoard()`:
4 колонки (Новые / В работу / В работе / Завершённые = new/received/processing/
completed — маппинг подтверждён точным совпадением подписи `processing`
"🟡 В работе" названию колонки 3); колонки 2/3 делятся на ячейки по испытателям
лабы (`lab_members.role IN ('lab_operator','lab_admin')`, ростер — новый
`fetchLabRoster`/`testersOf`/`myRoleIn`); колонка 4 показывает завершённую
заявку 10 РАБОЧИХ дней от нового `completed_at` (`withinCompletedWindow`,
Пн–Пт), затем карточка пропадает с доски (сама заявка не трогается — видна на
плоской «Результаты»).
- **Правило перетаскивания/назначения** (сервер — единственная точка входа
  `POST .../kanban-move`, см. `lab-service/AGENTS.md`, `kanban.go`): руководитель
  лабы (глобальная роль admin/superadmin) двигает и назначает свободно;
  испытатель двигает только уже назначенные ему карточки между СВОИМИ ячейками
  колонок 2/3 (и дальше в 4), либо забирает СЕБЕ (не кому-то другому)
  неназначенную заявку прямо из колонки 1 — самозабор, уточнено пользователем
  отдельно от исходного текста задачи. Клиент (`renderFlatKanbanColumn`/
  `renderPerTesterKanbanColumn`/`performKanbanMove`) реализует ТЕ ЖЕ правила и
  для drag-and-drop, и для контролов в детали заявки (полный статус-select +
  select испытателя у руководителя; «📥 Взять в работу» у испытателя на
  неназначенной новой; ограниченный статус-select у испытателя на своей уже
  назначенной) — сервер всё равно перепроверяет, клиентские предикаты только
  скрывают заведомо отклоняемое.
- Новое поле `LimsRequest.assigned_to`/`completed_at`; удалён геттер
  `canEditStatus` (был `myRole !== ''` — теперь решает сервер по факту вызова
  `kanban-move`, а не единая клиентская проверка).
- `GET /lab-members?lab_id=` (сервер) теперь доступен любому участнику этой
  лабы, не только admin — испытателю нужно видеть состав всех ячеек, не
  только свою.

**2. Пять доработок блочного редактора протокола** (список из 6 задач
пользователя — задача 6, экспорт Word/Excel из модуля заявок, целиком в
sbe-requests, см. его `AGENTS.md`):
1. Таблица результатов — границы/центрирование теперь видны и в «Кратком
   виде» (не только в модалке протокола): контейнер обёрнут новым общим
   классом `.tn-protocol-html` (sbe-core, зеркалит серверный `<style>`, который
   в короткий вид не доходит — вставляется только `doc.body.innerHTML`).
2. **Статические таблицы в тексте + выравнивание абзаца.** Новый
   `RichNode.type = 'static_table'` (визуальный конструктор — пользователь сам
   задаёт число строк/столбцов, каждая ячейка — mini rich-text через
   `renderInlineEditable`, сразу получает bold/italic/индексы/плейсхолдеры
   бесплатно) — в отличие от `type: 'table'` (данные серий, авто-заполняемые
   столбцы). `RichNode.align` (''/center/right/justify) — на абзаце/заголовке,
   `renderAlignSelect`.
3. **Верхние/нижние индексы во всех rich-text элементах** (абзац/заголовок/
   пункт списка — все три идут через один `renderInlineEditable`): настоящие
   `<sup>/<sub>` через `document.execCommand('superscript'/'subscript')` (как
   Ж/К), `InlineNode.sup`/`.sub`. Для подписи колонки таблицы (plain `<input>`,
   `execCommand` неприменим) — прежний юникод-приём, вынесен в общий
   `src/ui/subsup.ts` (`toggleSubSupPalette`, был приватным методом `LimsView`).
4. **Перетаскивание пунктов списка** — тот же паттерн, что уже был у колонок
   таблицы (draggable-строка + ⠿-хэндл + dragstart/dragover/drop + splice).
5. **Полный номер заявки везде** (заголовок детали, модалка протокола, имя
   файла DOCX) — `fullRequestNumber()` (`customer_number`, с кодом проекта/
   лабы/метода). Карточки списков («Все заявки»/«Очередь лаборатории» —
   отдельный частичный откат по прямой просьбе пользователя) остаются
   единственным местом с коротким `lab_number`.
- Попутно: `downloadDocx` переведён на общий `downloadBase64File`
  (`sbe-core/src/utils/download.ts`) — тот же хелпер теперь и в sbe-requests
  (кнопки Word/Excel там, задача 6).
- `npx tsc --noEmit`/`npm run build` — чисто (Kanban-доска дополнительно
  проверена: `go build`/`vet`/`test` в lab-service, 10 новых юнит-тестов
  `canApplyKanbanMove`/`normalizeKanbanTarget` — все зелёные; деплой на VDS
  подтверждён, миграция применена, `curl .../health` → `{"status":"ok"}`).
  Версия 0.2.2 → **0.2.3**.

### 2026-08-23/24 — v0.2.2 (живое тестирование ГВ: свободный ввод grade, singleton-модалка протокола)

Продолжение живого тестирования метода ГВ (испытатель загрузил реальные данные,
пересчитал серию) — три находки на стороне клиента, все три пары с серверными
багами того же тестового цикла (см. lab-service `AGENTS.md`, запись
"живое тестирование ГВ вскрыло 3 независимых бага").

1. **Свободный ввод результата ветки правила классификации.** Поле "то
   показатель =" было жёстко ограничено выпадающим списком `determinableIndicators`
   метода (В1/В2/В3 у ГВ) — не давало написать вывод вида "Соответствует"/
   "Не соответствует" для правила сравнения с целевым показателем
   (`target_group_compliance`). Теперь текстовое поле — источник истины,
   `<select>` рядом — быстрая вставка (показатели метода + новый
   `COMPLIANCE_VERDICTS = ['Соответствует', 'Не соответствует', 'Не оценивается']`),
   сбрасывается на placeholder после вставки, не единственный допустимый вариант.
2. **Singleton-модалка протокола.** `showHtmlModal` была НЕ настоящим Obsidian
   `Modal`, а обычным `div`, вставляемым прямо в `bodyEl` без какого-либо
   контроля — повторные клики по кнопкам вида ("Краткий"/"Выписка"/"Протокол")
   плодили накладывающиеся друг на друга div'ы, не закрываемые ни ESC, ни кликом
   по фону ("плодятся много экземпляров вывода", жалоба пользователя). Новый
   класс `ProtocolHtmlModal extends Modal`; `LimsView.openProtocolModal` хранит
   текущий экземпляр и закрывает его перед открытием следующего. Мёртвые CSS-
   классы `.tn-lims-modal`/`.tn-lims-modal-head` удалены (заменены на
   `.tn-lims-protocol-modal`, ширина модалки).
3. **Очистка устаревших `.formula`/`.aggregation` при смене `fill_method`.**
   Конфигуратор атрибута менял `fill_method`, но не чистил поле предыдущего
   способа заполнения — сервер теперь игнорирует такие устаревшие поля (см.
   lab-service), но оставлять их в конфиге незаметно для пользователя — источник
   путаницы; чистим и на клиенте при переключении `fillSelect`.
4. `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.2.1 → **0.2.2**.

### 2026-08-23 — v0.2.1 (системные атрибуты: испытатель/даты/условия среды — универсальные, не per-method)

Продолжение проверки синонимов метода ГВ (по запросу пользователя загрузить и
сверить пример данных из `mail_records.jsonl`, папка Flam): выяснилось, что
`amb_temp`/`amb_pres`/`amb_moist`/`exp_date` были заведены СВОИМИ атрибутами метода
ГВ, хотя эти поля несёт КАЖДОЕ письмо-результат (Comb/Flam/FlamProp, см.
`json_attr.md`) независимо от метода — то же для `flam_inventor`/`flam_rep_date`/
`flam_date_material_in`, у которых вообще не было атрибута-адресата. Решение
пользователя: "инвентор он системный, как и другие перечисленные... заведи эти
атрибуты как универсальные, так же как например «Наименование материала»" +
"нужно сразу в справке в конфигураторе показывать список системных атрибутов" +
"все системные должны автоматически попадать в форму испытателя". Полное правило
зафиксировано отдельным разделом выше («Правило: системные атрибуты»).

1. **`SYSTEM_PLACEHOLDERS`** (`block-editor.ts`, экспортирован) — 7 новых полей:
   `inventor`/`report_date`/`samples_in_date`/`exp_date`/`amb_temp`/`amb_pres`/
   `amb_moist`. Резолвятся сервером (lab-service `resolveSystemPlaceholder`) из
   новых колонок `requests.*`, заполняемых автоматически при email-импорте
   результата (см. lab-service `AGENTS.md`).
2. **Справка в конфигураторе** — над блоком «Атрибуты метода» список всех системных
   полей (переиспользует `SYSTEM_PLACEHOLDERS`), чтобы админ не заводил их вручную
   на каждом методе.
3. **Форма для испытателя** (`renderOperatorFormPreview`) — теперь ВСЕГДА показывает
   раздел «Системные данные (заполняются автоматически)» тем же списком, независимо
   от того, что админ добавил в `operator_form.fields` для конкретного метода
   (раньше функция вообще ничего не рисовала при пустом `fields`).
4. `LimsRequest` (`types/lims.ts`) — 7 новых полей для параллелизма с Go `Request`
   (`inventor_id`/`report_date`/`samples_in_date`/`exp_date`/`amb_temp`/`amb_pres`/
   `amb_moist`); ничего в клиенте их пока не читает напрямую (значения нужны только
   серверу для резолва плейсхолдеров), добавлены для полноты типа.
5. `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.2.0 → **0.2.1**. Деплой
   lab-service (миграция `requests.*`, см. lab-service `AGENTS.md`) выполнен в этой
   же сессии, колонки/FK на `inventors` подтверждены `\d requests` на сервере.

### 2026-08-23 — v0.2.0 (блочный редактор протокола: rich-text + плейсхолдеры,
заменил секции; 4 бага живого тестирования; каскад атрибутов; защита от потери
несохранённых правок; колонка «Серия» + порядок колонок)

**Секции представления (v0.1.19, вчера) отвергнуты пользователем как неподходящая
модель**: "редактор не функциональный... нужно сделать редактор в котором пользователь
будет выбирать не показатели... а блоки информации" — эталон приложен пользователем
(реальный полный протокол с реквизитами/юридическим футером/описаниями, не только
таблицами). Согласовано 2 вопроса: (1) визуальный редактор (WYSIWYG) с чипами, не
markdown-токены; (2) DOCX поддерживает форматирование (жирный/заголовки) сразу, не
только текст+таблицы. Полный план — `C:\Users\adm\.claude\plans\tidy-humming-balloon.md`.

1. **Модель данных — блоки с rich-text AST**: `methods.presentation = {blocks:[...]}`,
   `DocumentBlock{id, title, content: RichNode[], chart_id?, show_in_ui/excerpt/protocol}`.
   `RichNode` — `paragraph`/`heading`/`bullet_list`/`table`; `InlineNode` — `text`
   (bold/italic) или `placeholder` (`source: system|attribute`, `attribute_id`,
   `agg` — обязателен для атрибута уровня experiment вне таблицы: avg/min/max/first/last,
   динамические серии обязаны идти только в таблице — прямое требование пользователя).
   Легаси-фолбэк — ДВА шага назад без SQL-миграции (плоский `{fields:[...]}` от
   2026-08-21 и секции `{sections:[...]}` от 2026-08-22 конвертируются в блоки на лету,
   см. `parseMethodPresentation` в lab-service `AGENTS.md`).
2. **`src/ui/block-editor.ts` (новый файл)** — редактор одного блока: строки
   абзац/заголовок/список/таблица (➕/✖/drag-reorder); текстовые строки — небольшой
   `contenteditable` (не единый canvas) с тулбаром Ж/К (`execCommand`) и
   🏷Плейсхолдер (`PlaceholderPickerModal` — системные/агрегированные/эксперимента
   с доп. шагом выбора агрегации); чип — `<span contenteditable="false">`; paste —
   только plainText (защита от протаскивания посторонней разметки). Таблица — НЕ
   contenteditable, структурный виджет выбора колонок.
3. **Сервер (`lab-service/protocol.go`, полностью переписан)** — рендер HTML+DOCX по
   AST: `resolvePlaceholder`/`renderInlineHTML`/`renderNodeHTML`/аналоги для DOCX;
   заголовки в DOCX — прямое форматирование прогона (`<w:b/>`+`<w:sz>`), не именованный
   стиль (нет `styles.xml`); списки — префикс "• " текстом (без `<w:numPr>`).
   `POST .../protocol?template=&format=html|full` — `format=html` не тратит время на
   DOCX. **`GET .../short-view` (JSON) удалён** — карточка результатов теперь вставляет
   HTML из `protocol?template=ui&format=html` (см. lab-service `AGENTS.md`).
4. **4 бага, найденные пользователем в живом тестировании** (после первой версии
   блочного редактора):
   - Дублирование блока/невозможность удалить: `renderBlocksList`'ов внутренний
     `redraw` не звал `container.empty()` перед перерисовкой — каждое структурное
     изменение ДОБАВЛЯЛО полный рендер поверх существующего DOM. Исправлено; заодно
     убраны теперь избыточные локальные редравы внутри `block-editor.ts` (внешний
     полный ребилд их всегда перекрывает).
   - Метод «ГВ»: `Cannot read properties of undefined (reading 'kind')` в
     `renderOperandEditor`/`renderBranchRows` — легаси `clause.compare_to` битый/
     отсутствует. Исправлено защитным guard'ом + optional chaining в валидаторе.
   - Метод «ГВ» (тот же корень): `Cannot read properties of undefined (reading
     'forEach')` в `renderSubjectRows` — легаси `rule.subjects`/`rule.branches`
     отсутствуют. Исправлено **единой точкой нормализации** при построении массива
     `rules` в `renderMethodConfigForm` (лечит branches/subjects/clauses/compare_to/
     grade сразу — предвосхищая вероятный третий баг того же класса до того, как он
     был бы найден).
5. **Каскад атрибутов + защита от потери правок** (жалоба: "создал атрибут → чтобы
   использовать в правилах/протоколе — сохранить, закрыть, открыть заново, только
   потом использовать"):
   - Новый атрибут/переименование теперь сразу видно в пикерах классификации,
     блоков представления и формы испытателя — без сохранения и переоткрытия
     (замыкание `onAttrsChanged`, forward-declared `redrawBlocks`/`redrawOperatorForm`).
   - `MethodConfigModal` перехватывает `close()` (Obsidian маршрутизирует через него
     ×/Escape/клик вне модалки) — при несохранённых правках показывает
     `UnsavedChangesModal` (Сохранить и закрыть / Закрыть без сохранения / Отмена).
     `renderMethodConfigForm` возвращает `MethodConfigHandle{isDirty, save}`
     (`buildPatch`/`savedSnapshot`, переиспользованы из логики кнопки «Сохранить»).
6. **Колонка «Серия» — обычная колонка, не hardcode**: раньше сервер всегда
   prepend-ил номер серии (1,2,3...) первой колонкой таблицы без права её убрать/
   переместить/переименовать (жалоба: "отсутствует опция создания колонки с номером
   серии"). Теперь `TableColumn.kind="series_no"` — выбирается из того же списка, что
   атрибуты, можно удалить/переименовать; новые таблицы получают её по умолчанию
   (поведение по умолчанию не меняется). **Порядок колонок** — drag-reorder (⠿),
   тот же паттерн, что у блоков/строк содержимого (жалоба: "отсутствует опция
   изменения порядка колонок").
7. ⚠️ **Поведенческий регресс от секций**: секции (2026-08-22) для `template=protocol`
   молча добирали несконфигурированные атрибуты алфавитным хвостом "Прочие
   показатели" — гарантия "полный протокол никогда не теряет данные". У блоков
   этой гарантии больше нет: протокол полностью авторский, показывается только то,
   что явно добавлено в блоки. Не восстановлено намеренно (не согласовывалось
   отдельно с пользователем при переходе на блоки) — если нужна такая гарантия
   для новой модели, требует отдельного решения.
8. **Объединённые колонки/ячейки таблицы** — вопрос пользователя, не реализовано.
   Рекомендация: группированные заголовки (label на несколько под-колонок) вписываются
   в модель естественно; merge ЯЧЕЕК по строкам — неоднозначен (ячейки авто-заполняются
   из серий, не вводятся руками) — ждём конкретного кейса от пользователя.
9. `go build`/`go vet`/`go test` (lab-service, включая новый тест на колонку
   `series_no` и обновлённые тесты легаси-фолбэка) — чисто; `npx tsc --noEmit`/
   `npm run build` (sbe-lims, sbe-requests) — чисто. Версия 0.1.19 → **0.2.0**
   (архитектурная переработка — до ближайшего `0.x.0`, по правилу версионирования
   корневого AGENTS.md). **Задеплоено на VDS** (`docker compose up -d --build lab`),
   health `{"status":"ok"}`.

⚠️ Как и в предыдущих сессиях по этому блоку — ergonomics-ревью, не финал: пользователь
явно попросил "позже проверим" вживую после деплоя, ожидать новых правок.

### 2026-08-22 — v0.1.19 (секции представления, 3 вида вывода, форма испытателя)

**По жалобе пользователя**: "все атрибуты формируют одну длинную сводную таблицу,
которую неудобно воспринимать" — эталон структуры, присланный пользователем, —
десктопные отчёты (`30244.html`/`30402.html`), разбитые на пронумерованные
тематические подразделы (своя мини-таблица/график/вывод на каждый), а не одна таблица.

1. **Секции вместо плоского списка полей**: `methods.presentation` теперь
   `{sections: [...]}` — каждая секция (`id`, `title`, `fields`, `charts?`) даёт свою
   мини-таблицу/резюме/график. Поле — `role: "table"|"summary"` (колонка по сериям
   vs одна строка "подпись: значение") + **3 независимых флага видимости** вместо
   прежних двух — `show_in_ui`/`show_in_excerpt`/`show_in_protocol`. Легаси-конфиги
   (плоский `{"fields":[...]}` от 2026-08-21) читаются как есть, оборачиваются в
   одну секцию "Показатели" на лету (без SQL-миграции данных) — см. lab-service
   `AGENTS.md`, `parseMethodPresentation`.
2. **Ровно 3 вида вывода** (по решению пользователя — "ровно 3, простые галочки",
   не расширяемый список шаблонов): «Короткий (UI)» / «Выписка из протокола» /
   «Полный протокол». Состав «выписки» НЕ зафиксирован программно — админ метода
   сам решает, что и в каком виде (таблица/резюме) туда включить (явное решение
   пользователя: "пользователь должен определять").
3. **Редактор** (`renderMethodConfigForm`, блок «Представление данных» и «Графики»
   переставлены местами — секции теперь могут ссылаться на уже настроенные
   графики): список секций (drag-reorder, заголовок, удаление), внутри каждой —
   строки полей (drag-reorder внутри секции, выбор роли, 3 галочки, выпадающий
   список «перенести в другую секцию» при секций > 1) и под-список привязанных
   графиков (выбор из `chart_configs` + те же 3 галочки). Кнопка «Сгенерировать
   протокол» заменена на 3 кнопки видов.
4. **Карточка результатов заявки и sbe-requests** переведены на новый эндпоинт
   `GET /requests/{id}/short-view` (см. lab-service) — секции строятся ОДИН РАЗ на
   сервере, оба клиента просто рисуют то, что пришло (не дублируют группировку).
5. **Форма для испытателя** (`methods.operator_form`, новый JSONB) — только
   конструктор схемы (какие атрибуты вводит лаборант, подпись, обязательность,
   подсказка) + read-only предпросмотр «как увидит испытатель»; реальный фронт
   ввода (мобильный/веб) не разрабатывался — явно вне рамок этой правки.
6. `LimsLlmAssist.draftPresentation` — ИИ пока не знает про секции, черновик
   оборачивается в одну секцию "Показатели" сразу после генерации.
7. **`announceUpdate` в ЦУП** (новое правило корневого AGENTS.md, 2026-08-22):
   `onload()` сравнивает версию с сохранённой `lastAnnouncedVersion`, публикует
   новость об обновлении один раз на версию через `getService('sbe-apstore')`.
8. `go build`/`go vet`/`go test` (lab-service) — чисто; `npx tsc --noEmit`/
   `npm run build` (sbe-lims, sbe-requests) — чисто. Версия 0.1.18 → **0.1.19**
   (manifest + package.json). Деплой lab-service на VDS — отдельным подтверждённым
   шагом (ещё не выполнен на момент записи).

⚠️ Как и классификация/DSL этой сессии — ergonomics-ревью, не финал: ожидать
дальнейших правок структуры секций/видов после того, как пользователь опробует
это в реальном интерфейсе.

### 2026-08-21 — v0.1.14 (конфигуратор методов: атрибуты/формулы/классификация/
представление; ИИ-черновик из стандарта; синонимы для email-импорта)

**Крупная сессия — конфигуратор методов, блоки 1-3 из исходного запроса пользователя.**
Сервер (`server_back/lab-service`) правился в этой же сессии — см. его `AGENTS.md`;
блокирующий баг чтения конфига (там же) обязателен для того, чтобы всё нижеперечисленное
реально видело/сохраняло данные, а не открывалось пустым.

1. **Блок 1 — атрибуты метода** (`input_parameters`, структура вместо raw JSON):
   `renderMethodConfigForm`/`renderAttributeRows` — построчный редактор (id, название,
   тип данных text/int/float/date/time/photo, способ заполнения ручной/прибор/расчёт,
   уровень эксперимент/агрегат). Для расчётных — поле формулы (DSL) + кнопка
   **«🤖 ИИ»** (мини-панель: описание задачи → `LimsLlmAssist.suggestFormula` →
   предложение для правки, никогда не вставляется автоматически). Для агрегированных
   без своей формулы — выбор атрибута-источника + принцип (среднее/минимум/максимум);
   сервер сам строит `formulas[]` из этого (`deriveFormulasFromAttributes`, см.
   lab-service `AGENTS.md`) — клиент formulas не редактирует, только видит (readonly
   `<pre>`).
2. **Блок 2 — правила классификации** (`classification`): построчный редактор трёх
   типов — пороговое (сортировка на сервере, показывается порядок
   `determinable_indicators` метода для наглядности), булево (условие + показатель
   если верно/иначе), **новый тип «соответствие целевому показателю»** (compliance —
   цель всегда `objects.characteristics.target_indicators` заявки, только тексты
   результата редактируются).
3. **Блок 3 — представление данных** (новое поле `presentation`, JSONB-колонка):
   drag-and-drop (нативный HTML5, без библиотек) редактор порядка/подписей/видимости
   столбцов для UI-таблицы результатов и протокола + живой предпросмотр; таблица
   результатов заявки (`renderResultsTable`) и генерация протокола теперь читают этот
   порядок (фолбэк — как раньше, для не сконфигурированных методов). Заодно
   структурный редактор графиков (`chart_configs`, было raw JSON) — карточки
   тип/оси/подписи/ряды, тот же паттерн.
4. **Построчный layout** (по прямому требованию пользователя): одна строка = ВСЕ
   свойства одного атрибута/правила в одном `tn-lims-flex`, не блоки друг под другом.
   Дважды ловил регресс раскладки — сначала `.tn-lims-input`/`.tn-lims-select`
   (`width:100%`) ломали флекс-строку (добавлен override с `flex:1 1 120px` +
   `overflow:hidden` — `<select>` в Electron игнорирует чистый `min-width:0`), потом
   **фикс не применялся вообще**, т.к. правился скомпилированный корневой
   `styles.css`, а не реальный источник `src/styles.css` (`esbuild.config.mjs`
   перегенерирует корневой файл на каждой сборке из `sbe-core` токенов + `src/
   styles.css` — прямые правки корневого файла молча терялись при следующем `npm
   run build`). Раскрываемые панели (ИИ-помощник, синонимы) внутри строки —
   `flex-basis:100%`, чтобы не втискивались в саму строку, а шли под ней.
5. **Импорт конфигурации из стандарта (ИИ-черновик)** — новые
   `services/rtf-to-text.ts` (свой конвертер, RTF-библиотек в проекте нет; cp1251
   `\'XX`-escape через `TextDecoder`, кодовая страница из `\ansicpg` файла, пропуск
   `fonttbl`/`colortbl`/`stylesheet`/`info`/`header`/`footer`/`pict`-групп,
   `\par`/`\row`→`\n`, `\tab`/`\cell`→`\t`; проверено на реальных ГОСТ 30244-94/
   30402-96 — читаемый текст, но числа во встроенных как WMF-картинки таблицах
   не восстановить, ИИ инструктирован не выдумывать такие значения) и
   `services/llm-assist.service.ts` (`draftAttributesAndClassification`,
   `draftPresentation`) — кнопка «📄 Импортировать из стандарта (ИИ)»: файл
   (.rtf/.txt) → черновик атрибутов+правил+представления заполняет те же
   структурные редакторы (никогда не сохраняется автоматически, только после
   «Сохранить»). Санитизация ответа ИИ — транслитерация/дедуп id с картой
   переименований (чтобы formula/aggregation.source не отвязались от своего
   атрибута), отброс нераспознанных `rule_type`/`data_type`. Явная инструкция в
   промпте: id — перевод смысла на английский, НЕ фонетическая транслитерация
   русских слов («flame_duration», не «dlit_plam_gor» — оба варианта разбирались
   на реальном примере с пользователем).
6. **Синонимы атрибута** (`MethodAttribute.synonyms?: string[]`) — кнопка «🔗
   синонимы» на строке атрибута открывает поле альтернативных raw-имён. Решает
   конкретную проблему: ИИ-черновик может назвать параметр иначе, чем уже
   зафиксировано в email-импорте десктопной ЛИМС (`canonicalFieldNames`/
   `knownRawFields`, lab-service) — вместо подгонки имени атрибута под legacy или
   риска расхождения synonyms дают явное маппинг-решение без переименования.
   Обрабатываются на сервере (`resolveResultKey`, приоритет выше глобальных карт) —
   см. lab-service `AGENTS.md`.
7. **Настройка модели LLM** (`SbeLimsSettings.llmModel`, дефолт `gpt-5.6-luna`) —
   `sbe-llm` сам модель не хранит (её выбирает каждый потребитель, как в
   sbe-mailer/sbe-presentations), без неё шлюз chadgpt.ru не гарантирует формат
   ответа, `completeJson` мог падать. Поле в настройках плагина + `LimsLlmAssist.
   resolveModel()`.
8. **Найденный вместе с блоком 3 баг**: `protocol.go` строил список колонок
   протокола через `keys := map[string]bool{}` — обход `map` в Go недетерминирован,
   порядок столбцов был случайным от рендера к рендеру. Блок 3 (`presentation`)
   чинит это как побочный эффект для ВСЕХ методов (алфавитный фолбэк для
   несконфигурированных), не только сконфигурированных явно.
- Новые типы `types/lims.ts`: `MethodAttribute` (+`AttributeAggregation`),
  `ThresholdClassificationRule`/`BooleanClassificationRule`/
  `ComplianceClassificationRule`/`ClassificationRule`, `PresentationField`/
  `MethodPresentation`, `ChartConfig`/`ChartSeriesConfig`.
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.13 → **0.1.14**.
- ⏳ Не проверено «живьём» на реальном сервере: backend lab-service с этими правками
  **не задеплоен** (ждёт отдельного подтверждения) — до деплоя конфигуратор у
  СУЩЕСТВУЮЩИХ методов будет открываться пустым (старый прочитанный баг), новый
  импорт-из-стандарта не требует backend вовсе и должен работать уже сейчас.

### 2026-08-21 — v0.1.13 (карточка заявки: показ выбранного целевого показателя)
- **По следам переработки формы заявки в sbe-requests** (см. `sbe-requests/AGENTS.md`
  v0.1.14 — «Определяемые показатели» заменены на обязательный «Целевой показатель»,
  один выбор на метод, хранится в `objects.characteristics.target_indicators:
  {methodId: значение}`): карточка заявки теперь показывает выбранное значение —
  «🎯 Целевой показатель: {значение}» (если задано для текущего метода заявки).
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.12 → **0.1.13**.

### 2026-08-21 — v0.1.12 (карточка заявки: все детали, как у заявителя; переход объект → заявки)
- **По просьбе пользователя**, 3 пункта:
  1. **Карточка заявки** (`renderRequestDetail`) наполнена всеми полями, которые видит
     заявитель в sbe-requests — раньше показывалось только «Статус · Заказчик» одной
     строкой. Теперь сразу под номером единым блоком: номер заказчику, проект, группа,
     объект, ЕКН, внешний идентификатор, характеристики объекта (номер партии/
     идентификатор образца), приоритет, цель испытания, лаборатория, владелец, даты
     создания/обновления, статус, описание. Для этого:
     - Добавлены `LabProject`/`LabGroup` в `types/lims.ts` и `listProjects()`/
       `listGroups()` в `sync.service.ts` (`GET /api/lab/projects`/`/groups`,
       `viewer`-доступ — у sbe-lims раньше не было способа их запросить вовсе).
     - `LimsView` кэширует `objects`/`projects`/`groups` (загрузка в `initShell()`,
       обновление в `syncAndRender()` — та же кнопка «🔄 Синхронизация» v0.1.11).
     - Новые хелперы `objectName`/`projectName`/`groupName`/`priorityLabel`/
       `purposeLabel`/`formatDate` — те же подписи, что в `sbe-requests`.
     - Новый класс `tn-lims-mb12` (styles.css) — по образцу `tn-req-mb12`.
  2. Строка **«📧 Внешний идентификатор»** — `external_id` (номер заявки из
     письма-трекера LPITrack) с восстановленным префиксом `LPIZAYAVKINAPRO-` только
     для отображения; в БД хранится без префикса (см. `sbe-requests/AGENTS.md` за
     подробным объяснением, почему это важно для дедупа живого приёма почты).
  3. Кнопка **«→ Заявки»** в справочнике «Объекты» (`renderObjects`) —
     `showRequestsForObject()` переключает на список заявок, отфильтрованный по
     объекту. Понадобилось после объединения дублей объектов в lab-service
     (2026-08-21, 469 → 134 объекта): у объекта-плейсхолдера «Без названия» теперь
     129 заявок, без перехода не отличить, какая заявка к нему относится.
- Отдельно: список заявок (`renderRequests`) раньше не сортировался вовсе (порядок
  как вернул сервер). Теперь всегда сначала по `number_year` убыв., затем по
  `number_seq` убыв. — новые заявки сверху, самый большой номер текущего года —
  первая строка. Тот же порядок, что уже был в `sbe-requests`.
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.11 → **0.1.12**.

### 2026-08-21 — v0.1.11 (ребрендинг «LogicLAB.ЛИМС»; кнопка «🔄 Синхронизация»)
- **По просьбе пользователя**, 2 пункта:
  1. Отображаемое имя вьюхи (`getDisplayText()` и титул в шапке `buildShell()`)
     заменено с длинной фразы «Лабораторная информационная менеджмент система СБЕ
     ПМиПИР» на **«LogicLAB.ЛИМС»** — та же схема брендинга модуля, что уже
     используется в `sbe-requests` («LogicLAB.Заявки», см. `sbe-requests/AGENTS.md`).
     `manifest.json`/`name` не менялся (там и в sbe-requests — обычное описание, не
     бренд).
  2. Добавлена кнопка **«🔄 Синхронизация»** в сайдбаре (сразу под деревом навигации,
     класс `tn-lims-collapse`, без новых стилей) — `LimsView.syncAndRender()`
     перезапрашивает роль (`getMyPermission`), список лабораторий (`listLabs`) и кэш
     методов плагина (`SbeLimsPlugin.refreshMethods()`), затем перерисовывает текущую
     страницу. Аналог `syncAndRender()` в `sbe-requests`, адаптирован — у sbe-lims нет
     локальной БД/очереди push, только re-pull (сервер и так канон, кэшируются только
     `methods`/`labs`/роль на клиенте).
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.10 → **0.1.11**.

### 2026-08-20 — v0.1.10 (пересборка за sbe-core: SbeContactsApi)
- `sbe-core`: добавлены `SbeContactsApi` и `'sbe-contacts'` в `SbeServiceMap` — пересборка `main.js`, исходники плагина не менялись. Версия 0.1.9 → **0.1.10** (manifest + package.json).
- `npx tsc --noEmit` EXIT=0; `npm run build` OK.

### 2026-08-19 — v0.1.9 (фиксы по обратной связи: кнопка «Создать», список методов, описание метода)
- **Найдено пользователем**, 3 пункта (см.
  `docs/superpowers/plans/2026-08-19-sbe-lims-requests-followup-plan.md`, п.2-4):
  1. Кнопка «＋ Создать» в шапке — facade-заглушка с v0.1.1 (клик просто показывал
     Notice, ничего не делала). Наполнение давно закрыло все реальные разделы создания
     (заявки, методы, справочники, лаборатории в «Настройках») — верхняя кнопка стала
     непонятным мёртвым элементом. **Удалена** из `buildShell()`.
  2. `renderMethods()` фильтровал список методов по `this.labId` (текущей лабе в
     переключателе) — имело смысл при единственной лабе у метода; после введения
     `method_labs` (2026-08-19, тем же заходом) это стало ловушкой: только что созданный
     метод, если не привязан к ТЕКУЩЕЙ открытой лабе, просто не появлялся в списке
     (хотя корректно создавался — виден в справочнике sbe-requests, который не
     фильтрует). **Убран фильтр** — «Методы» показывает все методы; добавлена строка
     «Лаборатории: …» на карточке (имена всех лаб метода — не терять эту информацию
     после снятия фильтра).
  3. У метода не было поля «Описание» ни при создании, ни при правке (сервер уже
     принимал `description` в `POST /methods`, но форма его не собирала; `PATCH
     /methods/{id}` вообще не поддерживал `description`). Добавлено поле в обе формы
     (`renderMethodCreateForm`/`renderMethodConfigForm`) + сервер (см.
     `lab-service/AGENTS.md`); карточка метода показывает описание, если задано.
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.8 → **0.1.9**.

### 2026-08-19 — v0.1.8 (создание/правка лабораторий через UI; внешние лабы привязаны к внутренним)
- **Найдено по вопросу пользователя** «как создаются лаборатории?»: `POST /api/lab/labs`
  (superadmin) существовал на сервере с момента введения роли superadmin (2026-08-19,
  см. `lab-service/AGENTS.md`), но ни один плагин не вызывал его — единственная
  существующая лаборатория (`GG`) была создана вручную через `curl` с временным JWT.
  Не баг, а осознанно узкий MVP-скоуп предыдущей фазы (роли) — теперь закрыт. Следом
  пользователь явно попросил и **правку** лабораторий — сервер получил парную
  `PATCH /api/lab/labs/{id}` (та же валидация type/parent_lab_id, что при создании).
- `sync.service.ts`: `createLab({code, name, description?, type?, parent_lab_id?})` →
  `POST /labs`; `updateLab(id, {...})` (частичный PATCH) → `PATCH /labs/{id}`.
- `lims-view.ts`: сайдбар-элемент `labCreateEl` теперь — **список всех лабораторий**
  (только superadmin, `renderLabManagement()`/`renderLabRow()`; внешние подписаны
  «→ {родитель}» или «без родителя!», если `parent_lab_id` не проставлен) + кнопка
  «➕ Новая лаборатория»; у каждой строки — «✎», открывающая ту же форму
  (`renderLabForm(lab | null)`) в режиме правки (предзаполнена, кнопка «💾 Сохранить»).
  **Важно**: элемент сделан отдельным от `labBarEl` (переключателя лабораторий),
  который скрывается при 0/1 лабораториях (`this.labs.length <= 1`) — если бы список/
  создание жили внутри него, управлять лабораториями через UI было бы невозможно ровно
  тогда, когда это нужнее всего (0-1 лаба). `refreshLabSwitcher()` выделен в отдельный
  метод (переиспользуется `initShell` и формой).
- **По явному требованию пользователя (уточнено отдельно от исходного вопроса)**:
  внешняя лаборатория не может существовать самостоятельно — обязана быть привязана к
  внутренней; при этом внешняя лаба может иметь свои методы, отсутствующие у
  внутренней (расширяет её возможности), и заявки по ним должны попадать к сотрудникам
  внутренней (см. `lab-service/AGENTS.md`, «Внешние лаборатории привязаны к
  внутренним», — там же вся серверная механика резолвинга видимости через
  `parent_lab_id`). В форме: select типа (внутренняя/внешняя) + select родительской
  внутренней лабы, который появляется только при выборе «внешняя» и обязателен
  (проверка на клиенте до отправки — но финальная валидация всё равно на сервере);
  список внутренних-кандидатов в родители исключает саму редактируемую лабу (нельзя
  быть родителем себе).
- Переиспользована существующая CSS-разметка (`tn-lims-labbar`/`tn-lims-nav-label`/
  `tn-lims-flex`/`tn-lims-series-form`/`tn-lims-input`/`tn-lims-select`) — новых стилей
  не потребовалось.
- `types/lims.ts`: `Lab.parent_lab_id`.
- **Не сделано** (осознанно, не запрошено): методы, привязанные к нескольким лабам
  сразу (many-to-many) — обсуждается отдельно, требует решения о механике заявок
  (см. вопрос в этой же сессии), в этот коммит не входит.
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.7 → **0.1.8**.

### 2026-08-19 — v0.1.7 (испытатели/оборудование: правка+удаление; методы: создание+удаление)
- **Найдено по жалобе пользователя**: во вкладках «Испытатели»/«Оборудование» не было
  правки/удаления, у «Методов» — не было удаления, и было не понятно, как создавать
  новые методы (кнопки/формы просто не существовало). `sbe-lims/AGENTS.md` уже
  документировал справочники испытателей/оборудования как «CRUD editor+» — это было
  расхождение документации с реальностью (существовали только Create+Read и на
  сервере, и в плагине).
- **Сервер** (см. `lab-service/AGENTS.md`, запись «Справочники: правка/удаление»):
  `PATCH`/`DELETE /inventors/{id}`, `PATCH`/`DELETE /equipment/{id}` (editor+),
  `DELETE /methods/{id}` (admin). Удаление занятого справочника (используется в
  заявках/результатах/методах) отвечает понятной 409, а не голой 500.
- **Плагин** (`sync.service.ts`: `updateInventor`/`deleteInventor`,
  `updateEquipment`/`deleteEquipment`, `createMethod`/`deleteMethod` + приватные
  `patchEntity`/`deleteEntity`; `lims-view.ts`):
  - «Испытатели»/«Оборудование»: каждая строка таблицы — кнопки «✎»/«✖»; «✎»
    заменяет строку на инлайн-форму правки (без перехода на отдельную страницу),
    «✖» — удаление с `window.confirm`.
  - «Методы»: форма создания над списком (код/название/лаборатория из уже
    загруженного `this.labs`/показатели через запятую, admin); у каждой карточки
    метода — кнопка «✖ Удалить» рядом с «✎ Конфиг» (admin).
- `npx tsc --noEmit` EXIT=0; `npm run build` OK. Версия 0.1.6 → **0.1.7**.

### 2026-08-19 — v0.1.6 (роль superadmin учтена в клиенте)
- Вслед за сервером (см. «Модель прав ЛИМС» выше и lab-service/AGENTS.md): геттеры
  `canAdmin`/`canEditRefs` (`lims-view.ts`) теперь пропускают `superadmin` наравне с
  `admin`/`editor`; `settings-tab.ts` — панель прав видна superadmin (была только
  `admin`), добавлена опция `lab_auditor` в форму добавления сотрудника лаборатории.
- Версия 0.1.5 → **0.1.6** (manifest + package.json). `npx tsc --noEmit` EXIT=0;
  `npm run build` OK.

### 2026-08-19 — v0.1.5 (наполнение оставшихся пунктов дерева)
- **Сервер (lab-service, отдельно от этого коммита, задеплоено и E2E)**: перед наполнением
  найден и исправлен пробел видимости — сотрудник лаборатории (по `lab_members`) не видел
  заявки своих методов ни в `GET /requests`, ни в `GET /requests/{id}` (только owner/группа/
  admin); `requireLabAccess` проверял lab-скоуп только для результатов/графиков/протокола
  конкретной заявки, не для списка/детали. См. `server_back/lab-service/AGENTS.md`
  (запись 2026-08-19, «видимость заявок по lab-скоупу») — правки в `requests.go`.
- **Наполнение** (`src/ui/lims-view.ts`, `renderPage()` теперь диспетчер по всем 8 разделам
  дерева вместо заглушки):
  - «Очередь лаборатории» / «Результаты и протоколы» — тот же `renderRequests()`, что и
    «Все заявки», с фильтром по статусу (`new`/`received` и `completed` соответственно);
    метод обобщён до `renderRequests(filter?)`, фильтр запоминается в `currentRequestsFilter`,
    чтобы «← Назад» из карточки возвращал в тот же (отфильтрованный) список, а не в общий.
  - «Методы» — список из кэша `plugin.methods`; для admin — кнопка «✎ Конфиг» открывает
    JSON-редактор (4 textarea: formulas/classification/chart_configs/input_parameters),
    сохранение через уже существовавший `updateMethodConfig()` + `plugin.refreshMethods()`.
  - «Объекты» — **новое**: добавлен тип `LabObject` (`src/types/lims.ts`, зеркало
    `sbe-requests/src/types/requests.ts`) и `listObjects()` (`GET /api/lab/objects`, viewer)
    в `sync.service.ts`; список только на чтение (создание — прерогатива sbe-requests).
  - «Испытатели»/«Оборудование» — список (viewer) + форма создания (editor+), на уже
    существовавших `listInventors/createInventor`/`listEquipment/createEquipment`.
  - «Сотрудники» — список (отфильтрован по выбранной лабе) + добавление/удаление
    (`setLabMember`/`removeLabMember`, уже существовали); раздел целиком скрыт для
    не-admin, т.к. сам `GET /lab-members` на сервере admin-only.
  - Новые геттеры `canAdmin` (`myRole === 'admin'`) и `canEditRefs` (`editor+`) — точечная
    ролевая проверка для новых разделов. `canEdit`/`canEditStatus` (результаты/статус
    заявки) **не трогались** — точная проверка `lab_operator` для конкретной лабы клиенту
    недоступна без нового «моя роль в этой лабе» эндпоинта (`GET /lab-members` сам
    admin-only) — сервер всё равно валидирует по `requireLabAccess`, риска нет, только
    UX-огрубление (кнопка может быть видна тому, кому сервер её использовать не даст).
- Версия 0.1.4 → **0.1.5** (manifest + package.json). `npx tsc --noEmit` EXIT=0;
  `npm run build` OK.

### 2026-08-19 — v0.1.4 (номер заявки — сокращённый, лабораторный)
- **Найдено при проверке документации**: список «Все заявки» и заголовок карточки/протокола
  показывали `customer_number` — полный номер **заказчику**
  (`{projectCode}-{NNN}/{yyyy}-{labCode}-{methodCode}`, см. lab-service/AGENTS.md §9).
  ЛИМС — инструмент сотрудников лаборатории, для них документирован именно **сокращённый**
  номер лаборатории `lab_number` (`{NNN}/{yyyy}-{methodCode}`, без кода проекта/лаборатории).
  Заменено на `lab_number` (с фолбэком на `number_seq/number_year`, если пусто) в списке
  заявок, заголовке карточки заявки и заголовке протокола.
- Версия 0.1.3 → **0.1.4** (manifest + package.json). `npx tsc --noEmit` EXIT=0;
  `npm run build` OK.

### 2026-08-19 — v0.1.3 (external_id для переходного периода миграции)
- Тип `LimsRequest.external_id` добавлен вслед за sbe-requests/lab-service (см. их AGENTS.md) —
  номер из legacy email-трекера (`LPITrack`) для заявок переходного периода миграции с
  десктопной ЛИМС. ЛИМС только читает заявки — изменений в UI/sync.service не требовалось,
  только тип (поле проходит через `JSON.parse` ответа `/requests` автоматически).
- Версия 0.1.2 → **0.1.3** (manifest + package.json). `npx tsc --noEmit` EXIT=0;
  `npm run build` OK.

### 2026-08-19 — v0.1.2 (модель заявки под декомпозицию по методам; наполнение «Все заявки»)
- **Найдено и исправлено перед началом наполнения**: `LimsRequest`/дремлющий код
  (`renderRequests`/`renderRequestDetail`, написан на этапе v0.1.0, до подключения к дереву
  не использовался) был написан под **старую** модель заявки — `methods: Array<{method_id,
  customer_number, lab_number}>`. Сервер (lab-service) и sbe-requests перешли на новую модель
  ещё 2026-08-18 (декомпозиция по методам, см. lab-service/AGENTS.md): 1 заявка = 1 метод,
  поля `method_id`/`customer_number`/`lab_number` прямо на заявке, без массива. Код упал бы
  в рантайме на первом обращении к `req.methods` при подключении к дереву.
  Исправлено: `types/lims.ts` — `LimsRequest` приведён к актуальной форме ответа `/requests`
  (как `LabRequest` в sbe-requests, без клиентских полей `group_key`/`parent_id`/`sync_status` —
  они относятся только к офлайн-созданию в sbe-requests, ЛИМС всегда читает готовые заявки
  с сервера); `renderRequests()`/`renderRequestDetail()` переписаны на `r.method_id`/
  `r.customer_number` вместо цикла по `r.methods[]` (цикл по методам заявки убран — теперь
  один блок метода вместо цикла, так как метод один).
- **Наполнение**: пункт дерева «Все заявки» подключён в `renderPage()` — показывает
  `renderRequests()` (таблица, клик → карточка заявки: статус, ввод серии, расчёт, таблица
  результатов, графики, генерация протокола+DOCX) вместо общей заглушки. Остальные пункты
  дерева (Очередь лаборатории, Методы, Объекты, Результаты и протоколы, Испытатели,
  Оборудование, Сотрудники) — пока заглушка, наполняются следующими этапами.
- **Побочно найденный и исправленный баг вёрстки**: тот же дремлющий код использовал классы
  `tn-req-*` (заимствованные из sbe-requests — в бандле sbe-lims их нет, элементы рисовались
  бы без стиля). Переименованы в свои: `tn-req-meta`→`tn-lims-meta`, `tn-req-input`→
  `tn-lims-input` (для обоих уже были готовые правила), добавлены новые правила
  `tn-lims-p24`/`tn-lims-mb8`/`tn-lims-select`/`tn-lims-flex` (`tn-req-p24`/`tn-req-mb8`/
  `tn-req-select`/`tn-req-flex` — этих правил не было вообще ни у кого).
- Версия 0.1.1 → **0.1.2** (manifest + package.json). `npx tsc --noEmit` EXIT=0;
  `npm run build` OK.

### 2026-08-18 — v0.1.0 (создание, Этап 5-6 плана 2026-08-18-sbe-lims-plan)
- Скаффолд как sbe-requests. `LimsSyncService` (JWT из ЦУП app `lab`, заявки, результаты,
  расчёт, справочники испытателей/оборудования/сотрудников, конфиги методов PATCH,
  графики PNG, протокол HTML+docx, дашборд, permissions/me, таймауты 30с, 401/403-сообщения).
  `LimsView`: вкладки «Заявки» (список + карточка: серии, статус, графики, протокол),
  «Справочники» (методы с конфигами JSON, испытатели, оборудование, сотрудники лабораторий),
  «Дашборд». `refreshMethods()` при onload (pull методов из sync/pull). Settings (apiUrl +
  «Права доступа»). `publishService('sbe-lims', {open})`.
- `sbe-core`: добавлены `SbeLimsApi` (extends `SbeOpenableApi`), `'sbe-lims'` в
  `SbeServiceMap` и `getServiceName` → «ЛИМС».
- `npx tsc --noEmit` EXIT=0; `npm run build` OK (main.js 31KB, styles.css 19KB — склеен из
  tokens/components/own через build.onEnd).
- **Хвосты закрыты (2026-08-18)**: реестр `sbe-lims` добавлен в `registry.json`
  (hasView, tools, ownerEmail) и синхронизирован на сервер (`https://epyur.fvds.ru/registry.json`);
  `community-plugins.json` дополнен `sbe-lims`; git-репо `Epyur/sbe-lims` создано (init-коммит,
  запушен). После добавления `SbeLimsApi` в sbe-core все 8 SBE-плагинов пересобраны с бампом
  версий (см. центральный AGENTS.md).
- ⏳ E2E в Obsidian (открытие из ЦУП, результаты, графики, протокол) — после
  перезапуска Obsidian.

### 2026-08-18 — удаление дашборда из плагина
- По требованию пользователя дашборд удалён из sbe-lims (`LimsView`): убран таб «📊 Дашборд`,
  метод `renderDashboard()`, `syncService.getDashboard()`, тип `DashboardData`. Плагин теперь
  имеет вкладки только «Заявки» и «Справочники».
- Сервер не трогали: `GET /api/lab/dashboard` в lab-service остаётся (для будущего отдельного
  плагина-дашборда).
- `npx tsc --noEmit` EXIT=0; `npm run build` OK (main.js пересобран, hot-reload применит).

### 2026-08-18 — требования к переработке UI (задокументировано, реализация запланирована)
- Планируется переработка интерфейса «примерно как мокап»
  (`C:\Users\adm\Life_dev\tn-center-mockup\index.html`): **левая панель-дерево навигации по
  лаборатории** + переключение между лабораториями (для сотрудников с доступом к нескольким
  лабораториям и супер-админов). Референс десктопного ЛИМС (LogicLab):
  `C:\python_dev\LIMS_LPI` (Tkinter, дерево ProductGroup).
- **Отображаемое имя плагина в UI при этой переработке задать как:
  «Лабораторная информационная менеджмент система СБЕ ПМиПИР»** (сейчас `getDisplayText()`
  возвращает «ЛИМС»; имя должно быть видно в заголовке вьюхи/ЦУП).

### 2026-08-18 — v0.1.1: фасад UI (каркас «шапка + сайдбар + контент»)
- Согласовано с пользователем: **сначала фасад с заглушками, потом наполнение внутренностей**.
- `LimsView` переписан под фасад: шапка (титул «Лабораторная информационная менеджмент
  система СБЕ ПМиПИР» вместо «ЛИМС», crumb `{лаба} · {раздел}`, «＋ Создать» без действия);
  сайдбар-карточка (кнопка «Свернуть/Развернуть» → 64px, переключатель лабораторий из
  `GET /api/lab/labs` — скрыт при одной лаборатории, дерево навигации: группы «Заявки»
  (Все заявки / Очередь лаборатории) и «Лаборатория» (Методы / Объекты / Результаты и
  протоколы / Испытатели / Оборудование / Сотрудники)); контент-карточка: заголовок раздела +
  подзаголовок + **заглушка** («фасад готов, наполнение будет подключено следующим этапом»).
- Рабочие методы прошлой вьюхи (renderRequests, карточка заявки, renderResultsTable,
  протокол/графики/DOCX) сохранены в классе для подключения к узлам дерева.
- `sync.service.ts`: добавлен `listLabs()` (`GET /api/lab/labs`); `types/lims.ts`: добавлен
  тип `Lab`.
- `styles.css`: токены мокапа (`--accent:#E11B17`, карточки 16/10px) + классы фасада
  `tn-lims-topbar`/`-sidebar`/`-nav`/`-grp`/`-submenu`/`-content`/`-stub` и др. Поверх старых
  классов (заявки/справочники не сломаны).
- `npx tsc --noEmit` EXIT=0; `npm run build` OK (main.js 32KB). Версия поднята 0.1.0 → 0.1.1.

### 2026-08-22 — конфигуратор методов: типы boolean/photo, правила классификации
переработаны дважды (единая модель → branches/clauses), терминология, деплой

- **Атрибуты**: новый `data_type: "boolean"` (Да/Нет, хранится как true/false) и
  `"photo"` (URL — фото с мобильного терминала на S3, задел на будущее); фото и
  графики теперь можно вставлять в макет протокола (`charts.go`/`protocol.go`:
  `buildProtocolCharts`, `<img>` в HTML, текстовый плейсхолдер в DOCX — свой
  DOCX-writer пока не умеет медиа).
- **Загрузка атрибутов из JSON** — текстовое поле для вставки структуры (не файл):
  принимает готовый массив `MethodAttribute[]`/`{attributes:[...]}` ИЛИ плоский
  объект-пример реального результата email-импорта — в этом случае атрибуты
  ВЫВОДЯТСЯ из ключей/значений (`inferAttributesFromSample`/`guessDataType`), имена
  полей нормализуются через `CANONICAL_FIELD_NAMES` (зеркало `canonicalFieldNames`
  из `email_ingest.go` lab-service).
- **Синонимы атрибута** — кнопка «🔗 синонимы» у каждого атрибута для маппинга
  legacy raw-имён без оглядки на версию исходного JSON.
- **Правила классификации переработаны ДВАЖДЫ** по итогам разбора с пользователем:
  1. Три типа (`threshold`/`boolean`/`compliance`) объединены в один
     `conditions[]` с одним фиксированным атрибутом-источником на правило —
     пользователь отклонил: реальный запрос был на составную структуру как в
     Excel `IF`/`AND`/`OR`, со словом «Если» явно видимым в самой форме.
  2. Итоговая модель (см. specification.md): `ClassificationRule{output_name,
     aggregation_rule, branches[]}`, `ClassificationBranch{clauses?, join?, grade}`,
     `ClassificationClause{left: Operand, operator, right: Operand}` — оба операнда
     симметричны (атрибут/литерал/целевой показатель заявки). Ветки проверяются по
     порядку, первая совпавшая — результат; ветка без clauses — безусловное
     «Иначе». UI (`lims-view.ts`: `renderClassificationRows`/`renderBranchRows`/
     `renderOperandEditor`) рисует «Если»/«И»/«ИЛИ»/«то» как реальные слова формы.
- **Показатели метода (determinable_indicators) теперь редактируются** прямо в
  конфигураторе (список с ▲▼, а не только при создании метода) — на случай ошибки
  при первичной настройке; порядок ввода = ранг (первый — «выше»/«больше»).
- **Термин «лучше»/«хуже» изгнан из проекта** (см. «Правила» выше) — агрегация
  правил классификации: `none` (по умолчанию — НЕ сравнивать между сериями, брать
  значение текущей записи как есть) / `avg` / `min` / `max`; DSL `worst_grade`/
  `best_grade` → `min_grade`/`max_grade`.
- **Деплой lab-service**: обнаружено, что весь конфигуратор методов (включая баг
  2026-08-21, из-за которого `GET`/`sync/pull` не отдавали `input_parameters`/
  `classification` вовсе) не был задеплоен на VDS — отсюда «атрибуты/правила не
  сохраняются» при тестировании. Задеплоено (несколько раз, по мере правок
  результата классификации) — `docker compose up -d --build lab`, health OK.
  Попутно закрыт скрытый баг `handleUpdateMethodConfig` (`lims_refs.go`): PATCH
  безусловно перезатирал `formulas`/`classification`/`chart_configs`/
  `input_parameters`/`presentation` до `"[]"`/`"{}"` при отсутствии поля в запросе
  (не COALESCE, в отличие от `description`) — теперь честный частичный PATCH;
  добавлена поддержка `determinable_indicators` в том же PATCH.
- На машине разработки обнаружен и исправлен конфликт: команда `go` резолвилась в
  несвязанный npm-пакет (`gocli/go`) вместо настоящего Go — переустановлен через
  `winget install --id GoLang.Go` (1.26.7); см. `server_back/lab-service/AGENTS.md`.
- `go build`/`go vet`/`go test` (13 новых тестов классификации) — зелёные;
  `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.1.14 → 0.1.15.

### 2026-08-22 — v0.1.16: атрибут-помощник ИИ перенесён в строку, тип заполнения
"Правила классификации", над-/подстрочные символы в названии, карточки вместо
плоских блоков

- **Подбор id атрибута по названию перенесён из отдельной панели-списка прямо в
  строку атрибута** (по прямой правке пользователя: сначала была общая панель
  «Предложить атрибуты по описанию» со своим текстовым полем — пользователь
  попросил вместо этого значок рядом с полем «Название» в каждой строке).
  Значок 🤖 (title-подсказка при наведении) — по уже введённому названию ищет
  среди существующих атрибутов (этого и других методов) смыслово подходящий и
  копирует его id, либо просит ИИ предложить новый id (перевод смысла на
  английский, не транслитерация) — заполняется ТОЛЬКО поле id, остальные поля
  строки не трогаются. `LimsLlmAssist.suggestAttributesFromNames` (сохранён от
  прежней панели, переиспользован с массивом из одного элемента) — для каждой
  строки списка возвращает либо совпавший существующий атрибут (`matched`),
  либо черновик нового (`drafted`, санитизируется тем же `sanitizeAttributesWithRename`).
- **Новый способ заполнения атрибута — `fill_method: "classification"`**:
  атрибут, значение которого пишет правило классификации (не человек, не
  прибор, не формула). Валидация при сохранении: у атрибута с этим способом
  заполнения ОБЯЗАТЕЛЬНО должно быть правило классификации с `output_name`,
  равным его id — иначе ошибка с подсказкой создать правило или сменить способ
  заполнения (раньше было решительно не очевидно, как атрибут должен получать
  значение классификации).
- **Над-/подстрочные символы в человекочитаемых названиях атрибутов**: значок
  «x²» рядом с полем «Название» открывает палитру юникод-символов
  (⁰¹²³⁴⁵⁶⁷⁸⁹/₀₁₂₃₄₅₆₇₈₉) для вставки по месту курсора (CO₂, м³ и т.п.) —
  единственный переносимый способ без rich-text-редактора, работает
  одинаково в UI/протоколе HTML/DOCX.
- **Визуальные карточки** (по просьбе пользователя — визуально согласовать с
  тем, как задачи оформлены в sbe-tasks карточками): `.tn-lims-method`
  (методы/правила классификации/ветки правил/лаборатории) чинится и получает
  фон+рамку+радиус по токенам sbe-core (`--tn-card`/`--tn-border`/`--tn-radius`)
  — раньше рамка ссылалась на **несуществующую** переменную
  `--border-divider-default` и никогда не отображалась. Список заявок
  (`renderRequests`) переведён с `<table class="tn-table">` на кликабельные
  карточки `.tn-lims-req-card` (hover — как у `.tn-task-card`).
- `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.1.15 → 0.1.16.

### 2026-08-22 — v0.1.17: конфигуратор метода в отдельном окне; фикс вставки
формулы от ИИ; синхронизация представления при правке атрибутов

- **Баг: «Вставить в формулу» (ИИ-помощник, блок 1) не давал сохранить атрибут.**
  Кнопка меняла только видимый текст поля (`formulaInput.value`), а не сам
  `attr.formula`, который читает валидация перед сохранением — тот обновлялся
  только по DOM-событию `change`, а программная вставка значения такое событие
  не порождает без блюра поля. Формула визуально выглядела вставленной, но
  «Сохранить» отвергал атрибут как «без формулы» (обнаружено пользователем:
  ручной ввод той же формулы сохранялся нормально — это и указало на разницу
  между «видно в поле» и «записано в модель»). Исправлено: кнопка теперь пишет
  `attr.formula` напрямую.
- **Конфигуратор метода перенесён в отдельное модальное окно** (по прямой
  просьбе пользователя — раньше форма разворачивалась прямо под карточкой
  метода в списке методов). Причина переноса — тот же класс проблем: кнопка
  «Сохранить» вызывала `renderMethods()` (полная перерисовка списка), которая
  стирала `bodyEl` вместе с открытой инлайн-формой — окно визуально
  «сворачивалось» сразу после сохранения, хотя данные сохранялись верно.
  `MethodConfigModal extends Modal` рендерится вне `bodyEl`, поэтому фоновое
  обновление списка методов его больше не касается — форма остаётся открытой
  сколько угодно после сохранения. Ширина — `min(1100px, 92vw)` (обычная
  ширина модалки Obsidian слишком мала для строк атрибутов/правил с
  несколькими колонками), тело — скроллится, не растягивает окно на весь экран.
- **Попутно найден и исправлен смежный баг**, который и привёл к переносу в
  модалку: добавление/удаление/переименование атрибута (кнопка «➕ Добавить
  атрибут», ✖ в строке, значок 🤖 подбора id, ручная правка поля id) никогда
  не пересинхронизировало блок 3 «Представление данных» — presentationFields
  обновлялись только при полной загрузке JSON/ИИ-черновика. Пользователю
  приходилось нажимать «Сохранить» и заново открывать конфигуратор, чтобы
  новый атрибут появился в списке столбцов протокола. Теперь каждое такое
  изменение сразу вызывает `syncPresentationFieldsToAttrs` + перерисовку блока 3
  в той же сессии редактирования.
- `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.1.16 → 0.1.17.

### 2026-08-22 — v0.1.18: правила классификации переработаны в третий раз —
неявная левая часть + динамическая таблица subjects; ⚠️ эргономика ещё не
зафиксирована пользователем как окончательная

- **⚠️ ВАЖНО для следующих сессий**: пользователь явно подтвердил, что и
  расчётные функции (DSL-формулы), и правила классификации СЕЙЧАС проверяются
  ИМ на удобство/логику структуры UI, а НЕ на реальное функционирование —
  фактическая корректность вычислений/классификации будет проверяться ОТДЕЛЬНО,
  после того как эргономика будет выстроена и подтверждена. Т.е. то, что код
  собирается и тесты (results_test.go) зелёные, означает только «модель
  внутренне непротиворечива» — это НЕ подтверждение того, что итоговый UX уже
  финальный или что реальные расчёты на реальных заявках проверены. Не удивляться
  дальнейшим правкам структуры правил классификации/формул — это ожидаемо, а не
  сигнал, что предыдущая работа была сделана неправильно.
- **Правила классификации, версия 3** (после двух версий этой же сессии — см.
  записи выше): убрана «нефункциональная» строка результата/агрегации целиком;
  из самой схемы условий убрано упоминание конкретных атрибутов — «Если [знак]
  Б (И/ИЛИ …), то показатель» теперь ОДНА схема на всё правило, без привязки к
  атрибуту-источнику внутри условий (левая часть каждого сравнения — НЕЯВНАЯ).
  Вместо `output_name`/`aggregation_rule` на правило — динамическая таблица
  `subjects: {input_attribute_id, output_attribute_id}[]`: одна и та же схема
  условий применяется ПО ОТДЕЛЬНОСТИ к каждой строке (пользователь: «прогоняем
  оба списка через циклы») — можно оценивать сразу несколько атрибутов одним
  набором условий. Свод по нескольким сериям убран целиком (пользователь:
  «всегда не сравнивать») — атрибут всегда читается из текущей записи как есть.
  И «Оцениваемый атрибут», и «Куда записать результат» — теперь оба select из
  атрибутов метода (по просьбе пользователя результат тоже выбирается из
  списка, не вводится текстом).
- `results.go`: `applyClassification` → `applyRuleToSubjects` (по одному правилу
  на все subjects), `evalClause`/`evalBranch`/`evaluateBranches` принимают
  `subjectValue` явным параметром вместо `clause["left"]`. Убран `classifyCtx.
  seriesValues`/`agg` и мёртвый код агрегации (`aggregateNumeric`,
  `collectParamValues`) — агрегации больше нет вовсе.
  `results_test.go` переписан (13 тестов, включая новый
  `TestApplyRuleToSubjectsMultiple` — ключевой сценарий «разные subjects
  получают разные grade»). `go build`/`go vet`/`go test`/`gofmt -l` — чисто,
  **задеплоено на VDS**, health OK.
- `npx tsc --noEmit`/`npm run build` — чисто. Версия 0.1.17 → 0.1.18.

## Статистика ошибок и отступлений

- Нарушений правил нет: 0 `any`, 0 `fetch` (только `requestUrl`), 0 инлайн-стилей,
  `window.setTimeout` корректен (в `Promise.race` с очисткой в `finally`),
  все `catch(e: unknown)` + `errorMessage()`.
- `npx tsc --noEmit` EXIT=0, `npm run build` OK (без предупреждений).
- Замечание: `main.ts` использует `JSON.parse(res.text)` в `catch` — нотификация не показывается
  (только `console.warn`), без деградации; результаты из pull загружаются без правок.
