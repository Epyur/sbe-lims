/** Путь заметки-справки в вольте (корень). Тот же паттерн, что REQUESTS_HELP_PATH
 * в sbe-requests (help.ts) — см. openHelp() в lims-view.ts. */
export const LIMS_HELP_PATH = 'ЛИМС — инструкция.md';

/** Текст справки. Копия файла в вольте; при отсутствии — создаётся кнопкой «?». */
export const LIMS_HELP_MD = `# ЛИМС — инструкция

## Общие принципы

- Плагин обслуживает работу лаборатории: заявки, ввод и расчёт результатов испытаний,
  протоколы, оборудование, права сотрудников. Заявки создаются в отдельном плагине
  **«Заявки на испытания»** — здесь их принимают в работу, вводят результаты и готовят
  протокол.
- **Одна заявка = один метод испытаний.** Если объект проверяется несколькими методами —
  это несколько отдельных заявок с общим номером партии/объекта, каждая со своим номером
  заказчику/лаборатории.
- Слева — дерево разделов: **«📋 Заявки»** (Все заявки / Очередь лаборатории), **«🧪
  Лаборатория»** (Методы, Объекты, Результаты и протоколы, Испытатели, Оборудование,
  Сотрудники), внизу — **«⚙ Настройки»**.

## Как открыть плагин

В ЦУП СБЕ (магазин): «Установленные» → «ЛИМС» → «Открыть».

## Заявки

- **«Все заявки»** — список заявок, доступных вам (по владению/группе/лаборатории).
- **«Очередь лаборатории»** — канбан-доска по статусам (Новая → В работе → Завершена).
  Перетаскивание карточки между колонками меняет статус.
- **Взять заявку в работу**: неназначенную заявку из «Новых» испытатель может забрать
  себе перетаскиванием в колонку «Принята» — она автоматически назначается на него.
  Переназначить чужую заявку или забрать уже назначенную кому-то другому — может только
  руководитель лаборатории (lab_admin).
- **Карточка заявки** — детали, смена статуса (для уже назначенного вам испытания),
  результаты по методу, кнопка **«📜 История»** (кто и когда менял статус/результаты —
  список сворачиваемый, подгружается по клику), кнопка **«✉ Отправить письмо»**
  (результаты заказчику + дубль в LPITrack, если у заявки есть внешний номер из старой
  системы) с журналом уже отправленных писем.
- Если у лаборатории включена автоматическая отправка (настраивается в карточке
  лаборатории) — письмо заказчику/в LPITrack уходит само при завершении заявки, а
  уведомление о взятии в работу — при переходе в статус «В работе». Ручная кнопка
  работает всегда, независимо от этой настройки.

## Ввод результатов испытаний

- **С десктопа** (этот плагин): просмотр и правка уже введённых серий результатов —
  переключатель серий и кнопка **«🔄 Пересчитать всю заявку»** (пересчитывает формулы/
  классификацию по всем сериям метода, например если формулу поправили задним числом).
  Десктоп новые серии не создаёт.
- **С мобильного** (отдельный плагин «ЛИМС Мобайл»): создание новых серий по QR/номеру
  заявки — испытатель сканирует или вписывает номер и заполняет форму метода прямо у
  образца. Кнопка «➕ Добавить серию» сохраняет текущую серию (если менялась) и сразу
  создаёт следующую.
- **Секундомер** (если метод его использует, например испытание на воспламеняемость) —
  общий таймер на форму: кнопка события «зафиксировать» останавливает секундомер и
  записывает время в атрибуты метода; кнопки «лог наблюдений» (вспышка, начало
  карбонизации и т. п.) добавляют запись во времени, но таймер не останавливают.
  Секундомер продолжает идти и при переключении на другую серию, но интерактивен только
  в той серии, где был запущен.
- **Данные от прибора**: некоторые методы принимают hash от измерительного прибора —
  испытатель вписывает hash с экрана/QR прибора в форму результатов, сервер сам
  подставляет его данные в серию.

## Протоколы

- В карточке заявки (раздел «Документы») — готовый протокол испытания: просмотр в
  браузере (HTML) и скачивание в формате docx.
- Протокол собирается автоматически из результатов, формул, классификации и графиков,
  настроенных для метода — вручную его не редактируют, правится конфигурация метода или
  сами результаты.

## Оборудование и калибровка

- Раздел **«🧪 Лаборатория» → «Оборудование»** — справочник оборудования лаборатории,
  привязка оборудования к методам (какое оборудование каким методом используется).
- **Калибровка**: у оборудования, привязанного к методу, — записи калибровки (дата,
  значения калибровочных атрибутов). Для методов, где результат зависит не от одного
  числа, а от калибровочной кривой (например метод РП — тепловой поток от расстояния) —
  калибровка хранит набор точек, и результат испытания автоматически интерполируется по
  ближайшей действовавшей на дату испытания калибровке.
- Калибровка оборудования вводится так же, как результаты испытаний — включая мобильный
  ввод по QR оборудования.

## Настройка метода испытаний

Настраивается редко — обычно один раз при заведении нового метода лабораторией, силами
руководителя лаборатории (lab_admin). Раздел **«🧪 Лаборатория» → «Методы»** →
конфигуратор метода:

- **Атрибуты метода** — какие показатели вводятся/считаются/классифицируются по этому
  методу (данные испытателя, вычисляемые по формуле, результат классификации).
- **Форма испытателя** — какие атрибуты показываются в форме ввода, их порядок,
  подсказки, значения по умолчанию, условная видимость полей, настройка таймера
  (секундомер/захват события/лог наблюдений).
- **Формулы** — расчёт атрибутов по значениям других атрибутов (свой небольшой язык
  выражений: агрегация по сериям, условия, интерполяция по калибровочной кривой и т. п.)
  — настраивается лаб-админом при заведении метода, испытателю видеть эти формулы не
  нужно.
- **Классификация** — правила присвоения группы/категории по значению атрибутов (та же
  логика, что и в ГОСТах на метод).
- **Графики** — какие показатели строить на графике в протоколе (обычный график по
  сериям или график временного ряда — сырая кривая с прибора).
- **Калибровочные атрибуты** — если метод считает результат через калибровочную кривую
  оборудования (см. выше), здесь настраивается, какие атрибуты калибровки существуют.

## Права доступа

- Роли сотрудника внутри лаборатории (раздел «🧪 Лаборатория» → «Сотрудники»):
  - **lab_operator** (испытатель) — вводит и правит результаты испытаний, работает с
    заявками своей лаборатории.
  - **lab_admin** (руководитель лаборатории) — то же, что испытатель, плюс настройка
    методов/оборудования, управление сотрудниками, канбан-доска без ограничений
    (может переназначать чужие заявки), настройки лаборатории (автоотправка писем,
    адрес служебных писем).
  - **lab_auditor** — только просмотр (заявки, результаты, история изменений), без
    права редактировать.
- Общий уровень доступа к плагину (создание/просмотр вообще) настраивается отдельно, в
  ЦУП СБЕ — независимо от роли внутри конкретной лаборатории.

## Типовые вопросы

- **«Куда делась кнопка добавить серию на десктопе?»** — десктоп больше не создаёт новые
  серии результатов, только правит уже введённые. Новую серию создают с мобильного
  («ЛИМС Мобайл») по QR/номеру заявки.
- **«Почему пересчитанное значение не появилось сразу?»** — обновите страницу заявки
  (кнопка «🔄 Пересчитать» уже обновляет карточку сама; если значение всё равно не
  видно — проверьте, не пропущена ли сама формула в настройке метода).
- **«Почему письмо не ушло автоматически?»** — проверьте в настройках лаборатории
  галочку автоотправки и то, что у заявки есть адрес заказчика (и внешний номер — для
  дубля в LPITrack).
- **«Как узнать, кто менял результат?»** — кнопка «📜 История» в карточке заявки.
`;
